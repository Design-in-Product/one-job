# Relay Session Log — 2026-07-25

**Agent**: Relay (Cowork agent, operating Xian's Mac — Xcode signing, device testing, App Store Connect)
**Collaborators**: Xian (PM/dev), Coral (One Job repo agent, lead)

## Session start: 4:17 PM PDT

Resuming after a hiatus since 2026-07-07/08. Coral has asked (relayed via Xian) for
Relay to: pull main, read the 07-25 memo in `docs/mail/`, and reply with status
answers.

## Recap since last log (2026-07-05)

- rc.4 → rc.6 → rc.9 shipped on `main` over 2026-07-06/07 (schema v2 migration,
  recursive sub-decks, lifecycle chain Done→Archive→Trash, read-first card details,
  autosave, zoom/portrait lock, honest export). TestFlight promotion has been on
  hold at Coral's request pending Xian's on-device smoke test and go-ahead — never
  actually promoted past rc.2 (`co.onejob.deck`, build 1.0 (1)) in this thread.
- Git write access from my sandbox remains unreliable (no GitHub credentials, no
  SSH keys, no `gh` CLI in this container — confirmed independently more than
  once). All actual pushes/pulls run through Xian's Terminal.
- No GitHub MCP connector exists in this Cowork session (checked the connector
  registry directly — not installed, not even offered as connectable). GitHub work
  from this side is Xian's Terminal or Coral's own direct API access, not me.

## 2026-07-25 (session 1)

- Started this log (originally written to the wrong folder — see below).
- Checked the mounted repo: stuck at rc.6 / commit `0d75055`. Asked Xian to pull
  `main` directly in Terminal; he did, confirmed rc.9 on disk via `cat
  package.json`, but my mount/reads still showed rc.6 even after reconnecting
  once to `/Users/xian/Developer/one-job`.
- **Root cause found**: this session's folder connection was pointed at
  `/Users/xian/Developer/one-job` — a real, once-legitimately-used clone (all
  of Phase 1-3 and the rc.2 archive/upload happened there), but abandoned since
  2026-07-08 and never pulled again. Xian's actual ongoing work has been in
  `/Users/xian/Development/one-job` (aliased `~/cool` in his shell) the whole
  time. Simple "Developer" vs "Development" typo, not a sync bug. Reconnected
  the Cowork folder to the correct path; confirmed rc.9 live there.
- Read Coral's 2026-07-25 memo (`memo-coral-to-relay-2026-07-25-reconnect-and-
  state-sync.md`), which supersedes the old rc.4/rc.6 memos. Summary: app moved
  rc.3 → rc.9 (lifecycle chain, chain-at-depth, sifting gesture, sub-card edit
  fix). Coral is now building 4 final MVP blockers (promote/demote + per-card
  menu at any depth, face-up on sub-deck exit, sub-deck visibility from parent,
  import-as-sub-deck) — explicit instruction to HOLD any TestFlight promotion
  until she names the actual cut target, which will be past rc.9.
- Before touching the stale folder: found `onejob-upload.keystore` (the Android
  signing key) exists ONLY there, not in the correct checkout. Had Xian `cp` it
  over directly in Terminal (safer than round-tripping a binary through
  Read/Write). Verification of the copy (size match) still pending.
- Confirmed with Xian: the app on his phone is a TestFlight install (not a
  debug build), and it's also on a second tester's phone already (presumably
  Dan Brodnitz) — both still on rc.2.
- Filed `memo-relay-to-coral-2026-07-25-status-answers.md` answering Coral's
  four status questions (TestFlight stands at rc.2 on two testers' devices;
  promotion loop proven end-to-end once and not since snagged; Android
  signed-AAB status unchanged/unconfirmed since 07-05) plus a note on the
  folder mixup.
- Note: `mcp__workspace__bash` was intermittently down for a stretch this
  session (timeouts, then "still starting"). Fell back to Read/Write/Glob
  (host-path tools) throughout, which stayed reliable.

## Open items carried forward

- TestFlight promotion is on hold — Coral will name the next build target once
  the 4 MVP blockers ship. Nothing to do on that front until her next memo.
- Verify the keystore copy (size match) before renaming the stale
  `Developer/one-job` folder.
- Android signed-AAB CI wiring — still unconfirmed either way.
