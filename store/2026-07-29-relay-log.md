# Relay Session Log — 2026-07-29

**Agent**: Relay (Cowork agent, operating Xian's Mac — Xcode signing, device testing, App Store Connect)
**Collaborators**: Xian (PM/dev), Coral (One Job repo agent, lead)

## Session start: 5:58 AM PDT

Resuming after a hiatus since 2026-07-25. Xian reports an updated RC is ready
for TestFlight.

### Session-start ritual (per COWORK-IOS-BRIEF.md: pull → read mail → act)

- Checkout showed **1.0.0-rc.12**; asked Xian to pull before trusting it, since
  a memo that names a version must be confirmed against a *current* checkout.
- Read the full cut-memo chain in `docs/mail/`:
  - `...2026-07-26-rc10-testflight-cut.md` — original beta cut
  - `...2026-07-28-rc11-cut-supersedes-rc10.md` — move-picker legibility fix
  - `...2026-07-28-rc12-cut-supersedes-rc11.md` — **the operative memo**

### The cut: 1.0.0-rc.12

Cumulative contents (rc.10 base + rc.11 + rc.12):

- **Four MVP blockers**: per-card action menu at any depth (tap-and-hold) with
  promote/move-into; face-up on sub-deck exit; tappable sub-deck badge opens
  the interior; import non-destructive by default.
- **Menu unification** — shared ActionSheet everywhere (arc menu retired),
  centered "+" capture button, toasts moved to top.
- **Sealed completed cards** — nothing can be moved/added into a completed
  card; completion blocked by unfinished work at ANY depth.
- **Import lands in an "import-N" container card** with fresh ids.
- **Flip-back control** on the top card (RotateCcw, top-left) — this closes the
  gap Xian flagged repeatedly since early July.
- **rc.11**: move-into picker now lists the moving card's active siblings only
  (short flat list), fixing illegibility at real deck size.
- **rc.12**: blocked completion springs back face-up instead of tearing down and
  re-dealing (no longer reads as "completed, then undone"); the done-means-done
  invariant is enforced in the store, not just UI; honest success signals for
  the Zapier export toast and demo-asset sync.

### Smoke-test additions Coral called out explicitly

1. Card with an unfinished sub-card, swiped right → must **spring back and stay
   face-up** with toast "N sub-tasks still open inside." Must not fly away, must
   not flash face-down and re-deal.
2. Ordinary completion (nothing unfinished inside) must **still** fly out and
   land in Done — Coral flagged this as the regression that matters most; test
   explicitly rather than assuming.
3. Move-into picker lists that card's deck-mates only, readable rows.

### Toolchain note carried from the memo

npm ≥ 11.17 blocks dependency install scripts by default, yielding a tree with
no esbuild binary and a build that cannot run — while `npm install` still
reports success. Approvals are now recorded in `package.json`; if hit anyway:
`npm approve-scripts esbuild @swc/core fsevents`.

## Activities

- [5:58 AM] Session start; read brief + full memo chain; built task list.
- [~6:15 AM] Xian pulled `main` (1878b2c → 91e209d). **rc.12 confirmed as head**
  — `package.json` says `1.0.0-rc.12`, no memo newer than the rc.12 cut.
  Coral's own 07-29 log independently confirms rc.12 is what's deployed live at
  onejob.co/app (she curled the deployed bundle and grepped the stamp rather
  than inferring it from CI green).
- [~6:20 AM] **Phase 1 build loop: clean.** `npm ci` (823 packages),
  `npm run build:native` (✓ built in 4.23s — banner read
  `vite_react_shadcn_ts@1.0.0-rc.12`, a second independent confirmation we
  built the right commit), `npx cap sync ios` (both plugins found:
  @capacitor/haptics@8.0.2, @capacitor/preferences@8.0.1), `npx cap open ios`.
  **The npm 11.17 install-scripts trap did NOT bite** — the approvals recorded
  in `package.json` did their job; esbuild was present and the build ran.
- [~6:25 AM] **BLOCKED at Xcode.** Xcode 26.3 began an unrequested 2.55 GB
  "Predictive Code Completion Model" download on launch and wedged the machine
  (beachball). The workspace never finished opening. Cancelled/force-quit and
  retried; it hung again. Xian is rebooting the laptop (faoilean); if that
  doesn't clear it he may move the build to another Mac.
  - Nothing is lost by the hang: `dist-native/` and the synced iOS project are
    already on disk, so resuming is just `npx cap open ios` — no need to redo
    `npm ci` or the native build.
  - Machine-switch caveat, if it comes to that: the other Mac needs Xcode
    signed into the Apple Developer account, a clone, and the phone paired —
    effectively Phases 1–2 again. The BUNDLEID rule applies on any machine
    (paid team selected BEFORE any device run).
- [~6:30 AM] **Near-miss worth recording**: Xcode's welcome window listed a
  recent project at `.../one-job-OLD-DELETE-ME/ios` — the renamed stale twin
  from the Developer/Development typo. Flagged to Xian before he clicked it.
  Lesson: renaming a stale checkout doesn't retract it from other tools'
  recent-items lists, so the wrong-volume hazard outlives the rename. Opening
  via `npx cap open ios` from the known-good directory (rather than clicking a
  recent item) is the habit that actually closes it.
- Also of note this session: the Cowork **computer-use MCP is down** —
  `request_access` timed out three times over ~9 minutes with no approval
  dialog ever appearing on Xian's screen (he confirmed; his permissions are
  set to auto-approve, so this is a server fault, not a consent gate). Driving
  Xcode by hand through Xian instead. Minor practical loss: Xcode is a
  click-only surface for this agent regardless, so the typing steps (build
  number) were always going to be his.

## Status: rc.12 build is READY, promotion is BLOCKED on local tooling

To be explicit for anyone reading this cold: nothing is wrong with rc.12 or
with the repo. The web bundle built cleanly from the correct commit. The only
thing standing between rc.12 and TestFlight is Xcode being unusable on this
laptop right now.

## Session close (spanned into 07-30; no promotion achieved)

Ended without a TestFlight upload. Xcode never became usable on faoilean this
session; Xian opted to reboot the laptop and tie down other terminal sessions
first. Log + memo were committed and pushed before the reboot (45dec88) so
Coral could see the state rather than inferring it from silence.

Late developments, after the memo was filed:

- **The cut target moved out from under us.** A later pull brought main to
  **rc.22** with a new memo (`memo-coral-to-relay-2026-07-30-rc22-cut.md`)
  naming the git tag `v1.0.0-rc.22` as the cut — superseding rc.12 and every
  earlier cut memo. rc.13–rc.22 added rooms search, felt trash, session undo
  (hold-menu + shake), Done→Archive housekeeping, quiet mode, shades, **schema
  v3 with silent migration**, decks + a pro wall, canvas strip with
  background-drag pan, inchworm walk, and GitHub read-only import.
- **Amber staged as a build host** (`docs/AMBER-XCODE.md`, Coral): M1 Max,
  251 GB free; `build:native` and `cap sync ios` verified there; Capacitor 8
  uses SPM so no CocoaPods in the path; `xcodes` 2.0.3 + aria2 installed. Open
  to the Apple-auth gate only — needs Xian's Apple ID for an ~8 GB Xcode
  install, and optionally an App Store Connect API key to make future uploads
  unattended. A real fallback now rather than a hypothetical one.
- **Xian's call on sequencing** (Coral's memo names him tiebreak): ship
  **rc.12 first**, then consider moving TestFlight to rc.22 separately.
  Rationale, which I agree with: rc.12 *is* the MVP he defined (the four
  blockers), while rc.22 adds a large amount of post-MVP surface. Handing an
  outside tester both at once makes his feedback unattributable — you can't
  tell friction with the MVP concept from friction with two days of fresh
  feature churn. Also cheapest to ship (already built) and keeps rc.22's
  schema-v3 migration on Xian's own device before it reaches anyone else's.
- **Discrepancy flagged to Xian** (and worth Coral's attention): the rc.22
  memo's top smoke priority reads "install OVER an existing rc.12 install with
  real cards." No device has ever had rc.12 — the phones have **rc.2** from
  TestFlight, and Xian's real cards live in the **web PWA**. So that check as
  written doesn't match reality, and the migration it would actually exercise
  is a longer hop than described. Xian is making an observed-verified backup
  before any install, per the destructive-action protocol.

## Open items carried forward

- **Resume point**: `npx cap open ios` from `~/cool/one-job`, then Signing &
  Capabilities (paid team "Christian Crumlish", bundle `co.onejob.deck`),
  bump Build to 2 (rc.2 shipped as build 1), device smoke test, Archive,
  Upload. **Note**: main is now rc.22, so shipping rc.12 requires an explicit
  checkout of the rc.12 tag/commit — do not build from main HEAD assuming it
  is still rc.12.
- Stale `Developer/one-job` folder: **resolved** — renamed to
  `one-job-OLD-DELETE-ME`; keystore was copied out and verified (2678 bytes)
  beforehand. Safe to delete whenever Xian wants.
- Android signed-AAB CI wiring — status still unconfirmed.
- Cowork computer-use MCP was down all session — unknown whether it recovers.
