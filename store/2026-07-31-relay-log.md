# Relay Session Log — 2026-07-31 (Friday)

**Agent**: Relay (Cowork agent, operating Xian's Mac — Xcode signing, device testing, App Store Connect)
**Collaborators**: Xian (PM/dev), Coral (One Job repo agent, lead)
**Host**: faoilean (Xian's laptop), rebooted this morning

## Session start: 12:57 PM PDT

Xian rebooted the laptop; Xcode reports as working. Goal for this session: get
a TestFlight build up — the first since rc.2, which is what both testers are
still running.

### Carried decision (from 07-29/30): ship rc.12 first, rc.22 after

Coral's 07-30 memo names `v1.0.0-rc.22` as the cut, but also names Xian as
tiebreak. Xian's call is to promote **rc.12** first, because rc.12 is the MVP
he defined (the four blockers) while rc.22 carries a large amount of post-MVP
surface — decks, pro wall, canvas strip, quiet mode, shades, GitHub import,
and a schema v3 migration. Shipping both at once to an outside tester (Dan
Brodnitz, still on rc.2) would make his feedback unattributable.

**Consequence to respect**: main is now stamped rc.22. Shipping rc.12 means an
explicit checkout of the rc.12 tag/commit — building from main HEAD would
silently ship rc.22 while we believe we're shipping rc.12. This is the same
class of error the cut memos keep warning about, so the version gets confirmed
from `package.json` AND the build banner before archiving, as on 07-29.

## Activities

- [12:57 PM] Session start. Closed out the 07-29 log (which spanned into 07-30
  and ended with no promotion). Asked Xian to pull before reading mail, per the
  brief's pull → read → act ritual.
