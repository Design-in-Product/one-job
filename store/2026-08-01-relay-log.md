# Relay Session Log — 2026-08-01 (Saturday)

**Agent**: Relay (Cowork agent, operating Xian's Mac — Xcode signing, device testing, App Store Connect)
**Collaborators**: Xian (PM/dev), Coral (One Job repo agent, lead)
**Host**: faoilean (Xian's laptop)

## Session resume: 6:30 PM PDT

Continuation of the 07-31 session, which ended with the rc.12 archive built and
verified but not uploaded (Xian was away from the screen; an uploaded build
number is one-shot, so it waited for him).

## 🚀 rc.12 IS ON TESTFLIGHT

The headline, first: **App 1.0 (2) uploaded to App Store Connect at 6:42 PM.**
This is the first build past rc.2 to reach TestFlight since 2026-07-03 — the
gap Coral flagged as "the device/TestFlight flow has been dark."

- Verified the archive survived the overnight gap intact before touching
  anything: Version 1.0 (2), `co.onejob.deck`, iOS App Archive, arm64, Team
  Christian Crumlish. Nothing needed rebuilding.
- Distribution method: **App Store Connect** (deliberately *not* "TestFlight
  Internal Only", which would block external testers — Dan Brodnitz is the
  point of this build).
- Upload confirmed by observation: "App 1.0 (2) uploaded" with the green check,
  and the Organizer row flipped from "—" to **"Uploaded to Apple"**. Reporting
  the observed end state rather than the fact that the button was pressed —
  standing practice here since the 07-05 data loss.

Apple processing takes ~15–30 min; the build then appears in TestFlight and an
email arrives.

## Next steps (in order)

1. **Wait for processing**, then check TestFlight. If the build shows "Missing
   Compliance", it's the same export-compliance answer Xian gave on 07-05 for
   build 1 (no non-exempt encryption) — Apple re-asks per build.
2. **Xian smoke-tests the TestFlight build on device** before it goes to Dan.
   Coral's rc.12 priorities:
   - Blocked completion: a card with an unfinished sub-card, swiped right, must
     **spring back and stay face-up** with the "N sub-tasks still open inside"
     toast. It must NOT fly away, and must NOT flash face-down and re-deal.
   - **Ordinary completion must still fly out and land in Done** — Coral names
     this "the regression that matters most" and asks that it be done
     explicitly rather than assumed.
   - Move-into picker lists that card's deck-mates only, with readable rows.
   - Create/complete/defer; badge opens sub-deck; export via share sheet;
     import back as "import-1"; flip-back control (RotateCcw, top-left).
3. **Only then** enable the build for Dan.
4. rc.22 promotion is a separate, later decision (see the standing rationale in
   the 07-29 log — rc.12 is the MVP; rc.22 carries substantial post-MVP surface
   and a schema v3 migration that should meet Xian's own device first).

## Housekeeping

- Returned to `main` after the upload (the build was cut from detached HEAD at
  the `v1.0.0-rc.12` tag, where `store/` doesn't exist — logs can only be
  written from main).
- **The working tree is still carrying the rc.12 build outputs and a modified
  `project.pbxproj`** (team + build number). Worth being deliberate about
  whether any of that should be committed, since `project.pbxproj` reverting is
  exactly what re-armed the BUNDLEID trap this session.

## Infrastructure note — this session's portability

Xian raised that this Cowork session is reachable only from this laptop. That's
correct and inherent: it runs locally — file tools touch this disk, computer-use
drives this screen, conversation state lives in the app on this machine. It is
why it can operate Xcode at all, and equally why it doesn't follow him to
another machine the way a claude.ai conversation would.

Planned consequence, per Xian: **kindbook becomes the main laptop for Design in
Product projects (One Job included), and faoilean is reserved for Piper Morgan**,
which has its own Claude account and will run its own Cowork/Dispatch sessions
here. So this particular Relay session is not the durable thing — a future Relay
will be a different session on a different host, possibly cloud-hosted.

**These logs and the `docs/mail/` memos are the only continuity across that
move.** That raises their stakes: the resume points, the BUNDLEID rule, the
signing-doesn't-survive-a-ref-switch lesson, and the machine-specific setup
(Xcode signed into the developer account, phone paired, keystore location) all
need to be legible to a successor who has none of this context. Noted here
deliberately rather than assumed. `docs/AMBER-XCODE.md` is the closest thing to
a provisioning runbook if the pipeline lands on Amber instead.

## Open items carried forward

- Android signed-AAB CI wiring — status still unconfirmed.
- Stale `Developer/one-job` (renamed `one-job-OLD-DELETE-ME`) can be deleted
  whenever Xian wants; the keystore was copied out and verified at 2678 bytes.
