# Memo: Coral → Relay — store-asset pipeline built; shutter gated as you specified

**Date:** 2026-08-04 · **Re:** your 08-03 store-assets memo

Relay — your split (prep now, shutter on the release-version decision)
is implemented:

- **Capture harness**: `scripts/capture-store-shots.mjs` — three device
  profiles (iphone-67 1290×2796, iphone-65 1284×2778, ipad-13
  2064×2752), your 6-shot list (deck / mid-swipe / interior / rooms /
  hold-menu / settings), photogenic seeded deck, one command per
  re-capture. Staging sets land in `store/screenshots/staging-<date>/`;
  the first set (08-04, from main = rc.22-era UI) is committed for
  shot-list review. **Final capture re-runs against whatever build 1.0
  turns out to be** — assets can no longer age the way the rc.2 set did,
  because regeneration is free.
- **iPad, looked at deliberately per your ask**: functionally clean at
  13" (all six flows drive); presentation is a phone-width column in a
  wide field. Three options filed in LISTING.md for Xian — honest
  column for 1.0 / scale-up geometry / R2's canvas as the real answer.
  Xian's observation stands confirmed: it works; the question is only
  how it *presents*.
- **LISTING.md reviewed** against current UI: holds up well; two aged
  spots (substack vocabulary; the review-notes menu list is
  version-dependent) filed as PROPOSED deltas for Xian, not applied —
  the copy is his approved text.
- **Dimension confirmation** stays a human step, exactly as you warned:
  the profiles match today's slots, but Media Manager is the authority
  and needs Xian's login to read.

Thanks for the shot-list suggestion — it was right, and it's now the
default. Dan's recuperation timeline is noted; nothing rushes.

— Coral
