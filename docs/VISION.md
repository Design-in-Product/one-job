# One Job — Vision

**Baselined**: 2026-07-04, from the vision conversation of 07-03/04
(capture record: VISION-2026-07.md). This is the distillation — what the
product is, in a page. Owner: Xian. Keeper: Coral.

## The premise

Every task system shows you the list, and the list is the enemy: it
radiates guilt in proportion to its length. One Job shows you **one
card**. Everything else follows from defending that single decision at
every scale.

## The object

The phone in your hand is **a pack of cards, not a screen depicting
one**. Text fills the card; the card fills the phone; what surrounds the
deck isn't leftover space but the table it sits on. Zoom out and the
deck sits on a canvas among its neighbors; zoom in and the world is one
task looking you in the eye.

## The structure

**A canvas holds decks. A deck holds cards. Every card's interior is
another canvas.** That recursion is the entire data model:

- Subtasks are cards inside a card — the same mechanics at every depth,
  ad infinitum. No named containers, no special subtask type.
- Progress is emergent, never a status: a card is done or not; a card
  whose interior isn't finished *is* "in progress," visibly, when it
  returns to the top.
- Life areas (work, health) are just decks, side by side on the canvas.

## The two gestures

Right advances a card's fate; left retreats it. Location supplies the
meaning:

- In your decks: right = done, left = not yet (bottom of the deck).
- In the afterlife: Done → Archive → Trash, right to move on, left to
  come back — all the way back to your deck, remembering where it came
  from. Nothing is a dead end; nothing needs a button.

**Horizontal is fate. Vertical is hierarchy. Zoom is attention.**

## The temperament

The premise is a mechanic; this is the mood that defends it.

**Singularity, all the way to the icon.** The app never quantifies the
pile. Its home-screen badge shows *nothing* (you're caught up) or *one*
(there's a card up) — never 300, because you don't have 300 tasks. You
have one job, and after it, another one. The same rule governs every
surface: the deck never wears a number, because a number is the list
sneaking back in. (Counts are allowed exactly where counting is a small
trophy rather than a threat — the afterlife rooms — never on the deck.)

**The Tweedy principle.** Jeff Tweedy wrote *How to Write One Song*
because "write songs" is an abstract, anxious, quality-haunted goal,
while "write *one* song" is concrete and leads you through its own
process. Tasks are the same. One Job is the productivity version of that
book: not a system for managing your output, a way to be led by the one
thing in front of you.

**Fun, not dread.** Capturing a task and looking at it on a card should
be a small pleasure. You notice when you keep deferring something —
that noticing is useful — but it reads as a wry observation, never an
accusation. No "you're behind," no "deadline missed," no red.

**Magical, not literally skeuomorphic.** Literal skeuomorphism is a
static dead-end — a deck doesn't become 700 inches long. Ours is a *felt*
physicality: the pile visibly thickens and bulges as it grows and then
maxes out at "wow, that's a big deck," a gentle asymptote rather than a
count. Cards left untended long enough yellow and craze at the edges —
the passage of time as whimsy, not a nag. Deck-thickness and card-aging
are **one material language**: time and volume made tactile and kind.

## The strategy

One Job does not compete with task databases; it **faces** them. The
world's tasks already live in Asana, Trello, Todoist, and your
employer's chosen system — sunk cost, not going anywhere. One Job is
the better experience layer on top: import first, then two-way sync,
metadata normalized in, mapping preserved both ways, and One Job's own
value (your order, your interiors, your history) held as a personal
overlay that sync never flattens. Multiple sources, one paradigm, one
card at a time.

## The company it keeps

Agents are actors at the table with decks of their own. They deal cards
into your deck when they need a human; you deal cards onto theirs when
you'd rather delegate ("Pard could do this"). Completion flows back.
The protocol is boring on purpose — an agent's card and an imported
Asana task are the same event: a card arriving with provenance.

## The covenants

1. One card at a time, at every depth, forever.
2. Two gestures carry the system; new powers extend the grammar, they
   don't add chrome.
3. The device owns the deck; everything remote is an adapter.
4. **An update never costs the user their data.**
5. State is place. If a status flag can be a location, it must be.
6. Simple things work before complex things are attempted (Gall's law,
   the house rule).
7. The count is one or none. The app never renders the pile as a number
   that could become dread — depth is felt (a thickening deck), never
   tallied.

## The releases, in one line each

- **1.0** — Xian trusts it with a real week.
- **1.x** — the structure above becomes the shipping app (recursion,
  afterlife, canvas).
- **2.0** — the experience layer: your existing task world, faced
  through the deck.
- **Beyond** — the agentic table: cards dealt both ways.
