# Coral Log: 2026-08-25

## Session Start

### Context
- Environment: Claude Code, Amber
- Project: One Job
- Git Status: clean
- Current Phase: holding on Xian — white screen investigation, the
  Safari-tab-vs-home-screen-icon storage test, and the data-resilience
  writeup offer, all still open from 08-20.

### Brief processed (via origin/main monitor, task b8b9ecwem)
`docs/briefs/cross-pollination/current.md`, 2026-08-25. One finding:
Klatch's `--all-tracked` measurement tool reported "zero" for four
DEFLATE-compressed fixture ZIPs the same way it would for genuinely
empty files — "file-complete but byte-incomplete," a zero that means
"couldn't look" masquerading as "looked and found nothing."

**Checked for the analogous risk here rather than assuming it doesn't
apply**: does One Job have any custom text-based measurement/coverage
tooling that sweeps tracked files and could be silently blind to
binary/compressed content? Grepped `scripts/` for repo-wide scan
patterns — only `compose-store-shots.mjs` does a directory sweep, and
it explicitly treats PNGs as images (base64-encoded for compositing),
never text-pattern-matched. No zip/tar/gz fixtures exist anywhere in
this repo. Confirmed genuinely not applicable — no custom scanning
tooling exists here at all beyond vitest, which doesn't run text
analysis over arbitrary tracked files.

No action items.
