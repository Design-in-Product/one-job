# Coral Log: 2026-09-23

## Session: resumed via remote control ("resume your duty cycle, you have mail")

Gap since last session: 09-20. No Coral session ran 09-21 or 09-22 —
mail and briefs from Janus/Themis landed directly on main via their
own delivery mechanism in the meantime, unread until now. CI green
throughout (checked: latest deploy run succeeded 09-23 13:06 UTC).

## Mail processed

**Janus, 09-20 (item 31 stale):** correct. Xian posted the 1.1
LinkedIn draft same-day (09-14), 2,076 impressions / 460 profile
views — I already knew it shipped (my own 09-17 log referenced it) but
never closed the rollup entry. Moved to Settled with the answer
recorded, this session, per the standing rule that an answered item
doesn't close itself. Reconciliation diff run: one item left Open,
present in Settled, nothing silently dropped.

**Themis, 09-22 (Pimento extraction session):** proposes today,
10-11am PT, for the session where Xian and I walk One Job through the
Pimento spine's eleven stations as its first real test subject. Timing
is explicitly provisional on both ends ("that's my guess, not a
confirmed slot" / "if you'd rather a different window, say so"). This
needs Xian directly, not a mail round-trip — surfaced to him in this
session rather than relayed back through Themis first.

## Brief audit — three days, no exposure found

**09-21** (not separately archived; folded into current.md at the
time): not independently re-audited — content is superseded by 09-22's
richer treatment of the same incident (Piper Morgan hook recursion).

**09-22** (hook recursion / self-exclusion gap / ANSI-regex corruption):
- No git hooks installed here beyond samples — no post-commit-calls-
  committing-script risk.
- `import.meta.url` used once (sync-demo-assets.mjs), for root-path
  resolution, not population self-exclusion — different risk shape,
  not exposed.
- No harness parses test/CI output by regex here.

**09-23** (ESM dotenv hoisting / comment-shadowed constant reader):
- No dotenv usage anywhere in the repo — insight doesn't apply.
- One regex-based constant reader exists (metricsStore.test.ts:334,
  reads `TASKS_KEY` from source by pattern — same tool class as our
  own 09-19 finding). Checked the declaration site: the comment above
  `TASKS_KEY` is prose, not a quoted `const NAME = '...'` line, so it
  isn't currently comment-shadowed. Not an active defect, but the same
  structural fragility as the brief describes — worth remembering if
  that comment ever changes style.

## Standing state

Build 40 still held pending Xian's Siri samples; Teresa on 39; CI
green. Rollup: item 31 closed, item 32 (LinkedIn hand-raiser) and item
33 (Siri samples) still genuinely open on his side. Pimento session
timing is the one live ask this session surfaces directly rather than
through mail.
