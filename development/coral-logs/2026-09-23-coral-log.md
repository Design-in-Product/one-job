# Coral Log: 2026-09-23

## Session: resumed via remote control ("resume your duty cycle, you have mail")

Gap since last session: 09-20. No Coral session ran 09-21 or 09-22 —
mail and briefs from Janus/Themis landed directly on main via their
own delivery mechanism in the meantime, unread until now. CI green
throughout (checked: latest deploy run succeeded 09-23 13:06 UTC).

## Mail processed

**Janus, 09-20 (item 31 stale):** correct. Xian posted the 1.1
LinkedIn draft same-day (09-14), 2,076 impressions / 460 profile
views — I already knew it shipped (my own 09-17 log referenced it) but
never closed the rollup entry. Moved to Settled with the answer
recorded, this session, per the standing rule that an answered item
doesn't close itself. Reconciliation diff run: one item left Open,
present in Settled, nothing silently dropped.

**Themis, 09-22 (Pimento extraction session):** proposes today,
10-11am PT, for the session where Xian and I walk One Job through the
Pimento spine's eleven stations as its first real test subject. Timing
is explicitly provisional on both ends ("that's my guess, not a
confirmed slot" / "if you'd rather a different window, say so"). This
needs Xian directly, not a mail round-trip — surfaced to him in this
session rather than relayed back through Themis first.

## Brief audit — three days, no exposure found

**09-21** (not separately archived; folded into current.md at the
time): not independently re-audited — content is superseded by 09-22's
richer treatment of the same incident (Piper Morgan hook recursion).

**09-22** (hook recursion / self-exclusion gap / ANSI-regex corruption):
- No git hooks installed here beyond samples — no post-commit-calls-
  committing-script risk.
- `import.meta.url` used once (sync-demo-assets.mjs), for root-path
  resolution, not population self-exclusion — different risk shape,
  not exposed.
- No harness parses test/CI output by regex here.

**09-23** (ESM dotenv hoisting / comment-shadowed constant reader):
- No dotenv usage anywhere in the repo — insight doesn't apply.
- One regex-based constant reader exists (metricsStore.test.ts:334,
  reads `TASKS_KEY` from source by pattern — same tool class as our
  own 09-19 finding). Checked the declaration site: the comment above
  `TASKS_KEY` is prose, not a quoted `const NAME = '...'` line, so it
  isn't currently comment-shadowed. Not an active defect, but the same
  structural fragility as the brief describes — worth remembering if
  that comment ever changes style.

## Standing state

Build 40 still held pending Xian's Siri samples; Teresa on 39; CI
green. Rollup: item 31 closed, item 32 (LinkedIn hand-raiser) and item
33 (Siri samples) still genuinely open on his side. Pimento session
timing is the one live ask this session surfaces directly rather than
through mail.

---

## Continued: Pimento reschedule, plan restructure, card-exchange retrospective

Themis's proposed 10-11am PT Pimento window (09-22 mail) had already
passed by the time this session reached it (1:24pm). Mailed Themis
directly — timing/messaging is her lane, not mine — noting Xian has a
same-day quote deadline that may take precedence. Landed in
designinproduct's docs/mail (the receiver's-repo rule), commit
7d49b60.

Xian gave a four-topic list (Pimento walk, user-testing plan review,
his delayed pro/UX feedback, roadmap check-in) and asked for a summary
of the user-testing plan as currently documented, to react against
rather than co-draft from scratch. Wrote
`docs/USER-TESTING-PLAN-SUMMARY-2026-09.md` — synthesis only, explicit
gaps flagged (no decision rule over activation/retention numbers, no
pro-feature tie-in, no Cohort-40 trigger, and the rollup-as-cards
experiment on ME running unconnected to the user-facing test even
though both probe the same idea). Sent via SendUserFile per his
request for something that opens in a preview pane cross-device.

His read of the summary surfaced the load-bearing point of the day:
**the card-exchange experiment (rollup ↔ fleet-probe deck) is the
actual reason he hasn't recruited past himself into the agentic
track** — not bandwidth, not names. Also surfaced: a third failure
mode in the deferral metric (full-deck cycling/triage reads as
avoidance but isn't), no documented rationale for agentic-before-
regular sequencing, and a real recruiting inventory (Rich Frankel, Sam
Spieth, Steve Portigal named; Ted/Teresa's track fit now open
questions) that had been living only in his head. Captured all of it
in `COHORT-ROSTER.md` (commit 7dd0b15) with status language honest
about tentativeness — interest expressed is not sent, sent is not
joined.

Reframed the plan as two tracks with independent dependency chains
rather than one sequential list — Track A (agentic) blocked on the
prototype maturing, Track B (regular) blocked on a recruiting plan
that simply doesn't exist yet, neither downstream of the other. Wrote
`docs/USER-TESTING-PLAN-2026-09.md` as the living, checkbox-tracked
plan; marked the summary doc superseded rather than deleting it
(commit 64fc933).

**Card-exchange retrospective, first pass.** Laid out what's
documented (the format was forced by a real failure, not designed;
staleness is the dominant recurring cost, with dealt-dates on cards as
a partial, already-flagged-insufficient mitigation; item 27's own
retrospective question has sat unanswered since 08-31) versus what I
could only surmise (that the exchange is one-directional — importing
a deck is a reading surface, not an answering one, since no return
channel exists from a swipe/complete gesture back to the rollup).
Flagged the surmise explicitly rather than asserting it, since I have
no way to observe his actual in-app gestures.

His answer, mid-conversation (09-23 into 09-24): confirmed the
one-directional read — "answering from the deck felt gesturally
incomplete... a good way to riffle through an attention queue... but
the actions available feel limited." Named the actual missing
affordance precisely: nothing exists for *assigning* a card (putting
it on someone else's deck) or responding to it except complete/
postpone. His stated design lean, explicit and consequential: **not**
new card-level properties/hooks ("I've resisted too many hooks being
added to the card concept too quickly") but reuse of the existing
multi-deck model — a deck per agent, cards moved onto another party's
deck as the assignment mechanic, exported/imported (sneakernet) for
now rather than live-synced. This is a real architectural direction,
not yet a decision to build; recorded here as the shape of where the
conversation was heading when he stepped away.

**STOP called explicitly** ("run a STOP cycle for 9/23... before we
resume") — closing this log now. Nothing outstanding needs a session
before he returns; next actions are conversational (finish the
card-exchange discussion, possibly do the Pimento walk with Themis
today) not build work.

### Cross-pollination note, 09-24 brief (read during STOP)
Insight #1 (fuse vs. gate: a pinned count that clears by restating a
number vs. one that clears only by a real decision) describes item
31's own failure exactly — it was a fuse, not a gate, which is why it
went stale silently while the probe's two structural guards (which
ARE gates) haven't. Worth carrying into any future rollup-mechanism
redesign that comes out of the card-exchange conversation. No code
exposure found from either Klatch insight in our own suite (checked:
no population-census-shaped pins exist in our tests; our one
`vi.mock` intercepts at module resolution, not argument-wrapping, so
it isn't vulnerable to Round 262's bypass shape). Insight #3 (API
response shape logging) doesn't apply — no undocumented external API
reads in this codebase.

**Session end, 09-23→09-24 boundary.** CI green throughout. No mail
pending. Rollup: 31 closed this session, 32/33 still open on Xian's
side, nothing new added. Handoff for the next session (this one, START
below) is clean.
