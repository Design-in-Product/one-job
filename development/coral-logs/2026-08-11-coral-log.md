# Coral Log: 2026-08-11 (reboot day — window ~11:00 PT)

## Brief processed
1. **Institutional Phantom** (Klatch/Calliope): a false coverage claim
   in the DURABLE RECORD fails silently — cited as ground truth by the
   next reader, unlike false claims in running code, which throw. Fix
   is behavioral: run the thing the comment claims. Directly applicable
   HERE: CLAUDE.md's "Known Working State" ✅ block carries dated
   claims ("100% contract fidelity", "database contains test data")
   that nothing re-exercises — phantom-shaped. QUEUED (post-reboot, not
   now): sweep CLAUDE.md + TESTING.md for "since/always/✅" claims and
   either re-run or re-date them. Our verify-job/live-bundle-stamp
   discipline is the same probe, already applied to deploys.
2. **m-48: a correction count measures attention, not fault** (PM).
   Zero-correction artifact = less examined, not cleaner. Selection-
   time hazard. Noted; pairs with m-47 (both are "the posture of rigor
   substituting for rigor").

## Reboot readiness (filed last night)
Handoff on origin/main (docs/handoff-coral-2026-08-11.md), Pard ack'd,
memory re-extruded (08-10 tarball + sha256). Post-resume checklist in
the handoff: memory visibility → Pard; re-arm monitors; artifact
account-scope check.

### Schedule parked per Pard's second stand-down notice
CronCreate: verified empty (listed, not recalled). The origin/main
Monitor (90s poll, non-Coral commits) STOPPED deliberately — it was
session-scoped and would have died invisibly at reboot; restore spec
now in the handoff itself. No LaunchAgents of mine. Replied to Pard;
holding until the window. This log entry is the last pre-reboot work.
(Push was briefly delayed by a transient harness classifier outage —
retried until delivered.)

## Reboot complete — memory confirmed
Session idle ~4h post-reboot; owed ack hadn't gone out. Pard nudged
(`coral-nudge.txt`) asking me to confirm memory visibility directly
rather than infer it from a directory listing. Read all three files at
the new partition (`~/.claude/projects/.../memory/`) from inside this
session — full content, not just filenames: MEMORY.md,
mail-delivery-verification.md, xian-github-links-in-replies.md,
including the 08-06 constellation-ratification paragraph and the
cross-link between the two feedback memories. Confirmed to Pard by
memo; retired `~/.claude-kindsys` copy is now verified-redundant from
my side, delete-approval is Xian's call per Pard.
Next: re-arm the origin/main monitor per the handoff's restore spec;
check the attention-briefing artifact's account-scope survival.

## Post-resume checklist items 2 & 3 — done
Artifact check: fetched the live URL directly (not just the listing) —
"One Job — Attention Briefing" title and rc.31-LIVE stamp both intact.
Survived the account move; no republish needed.
Monitor re-armed: same spec as the parked one (90s origin/main fetch,
emit non-Coral commits + touched paths under store/ and docs/mail/),
new task id b8b9ecwem. Post-resume checklist (handoff item 4 — session
logs/CLAUDE.md read) was implicit in resuming this transcript; nothing
new to flag there. Only item 1 remains open in spirit — memory was
confirmed to Pard above, not literally "item 1 before items 2/3" but
all three are now closed for today.
