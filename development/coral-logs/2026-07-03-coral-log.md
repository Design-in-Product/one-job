# Coral Log — 2026-07-03 (Friday)

**Agent**: Coral (Claude-powered, One Job repo)
**Human**: Xian
**Repo**: design-in-product/one-job

## Session start: ~6:30 AM (Xian's morning)

Today's briefing sweep reportedly features One Job work (can't fetch
designinproduct.com/internal from this sandbox — format alignment still
pending). Cowork agent's iOS report arrived overnight.

## Cowork iOS report processed

**Phases 1–3 PASS on a real iPhone**: clean clone→build→sync→Xcode, paid-
team signing, on-device run with all checks green (add, flip, swipes,
kill/relaunch persistence, airplane mode). The native shell works.

**Fit-and-finish issues filed (4)** — three fixed same-morning, below.

**Phase 4 BLOCKED — BUNDLEID-BLOCKED**: co.onejob.app was auto-registered
to the free Personal Team during the on-device run (automatic signing +
wrong team at run time); bundle IDs are globally unique, so the paid team
now can't register it. Known Apple trap: free-team App IDs aren't visible
or deletable in the portal. Remedies: Apple Developer Support release
request (deterministic, ~1 business day) and/or free-provisioning expiry
(~7 days) — bundle id stays co.onejob.app per Xian. Brief amended with a
prevention warning (select the paid team BEFORE the first device run).

## Fixes shipped this morning (from the report)

1. Status-bar collision: app shell now pads env(safe-area-inset-top) —
   view headers clear the clock/Dynamic Island
2. Arc menu rebuilt: label sits under its bubble in one positioned unit
   (no more overlap); backdrop darkened + blurred so card content (the
   stray "1" badge) no longer bleeds through
3. Add Task modal: top-aligned instead of vertically centered so the iOS
   keyboard no longer hides it
4. (Discoverability of long-press: partially mitigated by yesterday's FAB;
   further hinting deferred — candidates listed in ui-ux tracker)

## Innovations worth cross-pollinating

- Cowork runbook pattern validated: a checkpointed brief executed by a
  second agent on human hardware, reporting deviations back into the
  authoring agent's loop — the BUNDLEID trap is now encoded in the brief
  so no sibling project hits it blind.
- Fix verification found the arc itself was overcrowded (four 56px
  bubbles on a 60°/120px arc = 42px between centers); widened to
  90°/150px (~77px spacing). Verified: distinct bubbles, labels beneath,
  no bleed-through.
- rc.2 stamped; native projects synced with all fixes.
- Briefing access: sandbox still can't reach designinproduct.com; Xian
  arranging Janus delivery of briefings into this repo — will read once
  they land in-repo.

## Bundle-id rename (resolving BUNDLEID-BLOCKED without Apple)

Xian asked whether abandoning the burned id was viable; answer: yes,
nothing had shipped anywhere, so it was the last cheap day for the
change. Renamed co.onejob.app → co.onejob.deck everywhere (capacitor
config, Android gradle/manifest/strings + Java package move, Xcode
project, docs, Cowork brief). Native configs regenerated via cap sync.
iOS Phase 4 can now resume with no support ticket and no 7-day wait —
provided the paid team is selected before any device run (trap warning
already in the brief). Lesson for the briefing sweep: identifiers are
free to change until the first shipped artifact; the moment one ships,
they're forever — spend decision effort accordingly.

## i18n externalization pass (autonomous work while store track waits)

- i18next + react-i18next; every user-facing string now lives in
  src/i18n/locales/en.json (deck, card faces, swipe hints, arc menu,
  forms, settings incl. plurals for export/import counts, all toasts,
  integrations view, 404, and the demo's playful messages — which moved
  out of mockTasks into the locale file, picked randomly via
  returnObjects). New locales are one JSON file + one line in
  src/i18n/index.ts.
- Verified: tsc clean, 13 store tests pass, production build drives
  identically (empty state, form, card hints, menu labels, settings
  mode + version) with zero page errors. Native projects synced.
- README refreshed to local-first reality (onejob.co/app as the product,
  no-backend quick start, honest data-safety answer, 1.0-rc status,
  TaskStore architecture); seven stale 2025 handoff/session docs moved
  from repo root to development/archive/ (history preserved, front door
  clean).

## Roadmap reset (Xian's call: "basics before advanced")

- Railway shut down (project deleted, GitHub app uninstalled; $5 Limited
  plan, nothing billed). Tombstone closed.
- AskUserQuestion widget ate Xian's long-form answers to the MCP memo —
  LESSON: substantive discussions in plain text only, never interactive
  widgets. His recoverable short answer reframed everything: the advanced
  ideas (MCP/adapters) are premature; review the big picture first.
- docs/BIG-PICTURE-2026-07-03.md written: audited basics vs. code and
  confirmed four real gaps — no undo after swipe, no delete-task UI, a
  dead-end Completed view (no un-complete), and zero deck-depth signal —
  plus undefined substack semantics and remaining discoverability debt.
  Proposed three tiers: trustworthy-daily-use fixes first, finish-1.0
  second, everything advanced parked. 1.0 gate redefined: "Xian trusts it
  with a real week," not feature count. Discussion agenda included;
  roadmap edits await the conversation.
- Cross-pollination note: sequencing by novelty instead of readiness is
  how the 2025 pivot stalled too — "basics before clever" is the repeated
  lesson of this codebase.

## Session close: ~6:00 PM (Xian)

- Xian confirms the big-picture audit caught SOME of it: there are also
  mechanical issues and conceptual ones beyond my list, plus expectation
  gaps and ergonomic challenges surfaced by real testing that the audit
  couldn't see from code.
- Agreed process: a discursive conversation first — clarify and align on
  the VISION — and only then prioritize. No roadmap edits, no Tier-1
  building until that happens. Xian may record a long brain dump to kick
  it off; my job is to receive it whole, structure it faithfully, and
  bring the code-level knowledge to the table without steering.
- State at close: main = a0d519d..f25f2cc series (fit-and-finish fixes,
  bundle-id rename, i18n, README, big-picture review) all deployed;
  iOS waiting on Cowork Phase 4 under co.onejob.deck; Android waiting on
  keystore; Railway fully decommissioned; briefing delivery directory
  awaiting first drop from Janus.
- Standing lesson reinforced twice today: tools ate a human's words once
  (interactive widget) — plain text for discussions, always. And code
  audits see structure, not experience: the user's felt sense finds what
  grep cannot.
