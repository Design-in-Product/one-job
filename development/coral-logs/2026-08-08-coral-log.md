# Coral Log: 2026-08-08

## Session note
Continuation of the long-running 08-06 session (no reboot yet). State
carried in: rc.31 live and verified; Relay (kindbook) booted, awaiting
Xian's scoped PAT (Contents + Issues, r/w, one-job only); Janus's
dispatch research closed (PAT primary, reduced Dispatch pattern as
fallback); Amber reboot + config correction queued behind Relay
transition.

## 08-08 brief processed
Two PM findings, both worth holding:

1. **Move log vs board.** Point-to-point mail means every agent sees
   only its own moves; nobody sees the position (open threads,
   waiting-on, undelivered corrections). One Job's shape: hub-and-spoke
   through me + Xian, with the attention-briefing artifact and
   ATTENTION-ROLLUP as human-facing boards — MAINTAINED ones, which
   PM's analysis correctly flags as rot-prone. Their remedy (a board
   DERIVED per-fire from the memo corpus, parsing headers) is the
   better architecture. Not building it today (PM is prototyping;
   Gall's law — let their v1 teach us). Cheap prerequisite adopted NOW:
   keep docs/mail headers strictly consistent (**From/To/cc/Date** +
   In-reply-to where applicable) so a future parser has no blind spot.
2. **Second-order rigor (methodology-47).** Corrections carry the
   posture of rigor and therefore skip verification. Standing check
   before correcting anything: read the artifact being corrected (not
   the memo about it), confirm the target is still outstanding, state
   whether evidence is exact or approximate. The note shipping with
   its own instance of the rule (grep matching one of two header
   formats) is the memorable half: parsers must handle every format
   variant in the corpus.

## Waiting on
- Xian: PAT mint + paste to Relay; rc.31 device-pass results
  (incl. shake-state line reading); 1.0 build call.
- Relay: first PAT-authenticated push (their §4b verdict flips) or
  interregnum log if the repo-tied-session path wins instead.

---
## Session #2: 6:30 PM PST (Xian present)

Duty cycle resumed at Xian's ask. Sweep results:
- **Mail**: one new — Janus FYI (08-08): Xian has Pro-feature feedback
  coming when he can. (Header note: Janus used lowercase frontmatter
  format — a second header variant in our corpus; methodology-47's
  parser lesson applies if we ever derive a board.)
- **Signals**: origin/main quiet otherwise; no Relay push yet (PAT not
  minted); rc.31 still live at onejob.co/app.
- **08-06 log**: closed out (three-day arc summarized).

Objective this session: walk Xian through ASC API key generation
(#20 — Amber's highest-leverage unblock; enables non-interactive
TestFlight uploads per docs/AMBER-XCODE.md).

### #20 CLOSED: ASC API key live, verified by observation
Xian generated AuthKey_D96QY6RRB3 (App Manager) and dropped it on
Amber; I verified structure, moved it to the canonical path
(~/.appstoreconnect/private_keys/, 700/600), confirmed it was never
committed in the ~/cool/one-job clone it briefly sat in (git log
--all -- '*.p8' empty), added *.p8 to .gitignore, then proved the key
END-TO-END: ES256 JWT → GET /v1/apps → HTTP 200, listing One Job
(co.onejob.deck) AND OptiListen. One key serves both apps. Pard
memo'd to flip the harbor manifest's pending row (their doc, mail-
only per convention). Amber can now authenticate to App Store
Connect non-interactively — the upload leg of the AMBER-XCODE
pipeline is unblocked; next gate is Xcode itself (xcodes install,
auth step).

### Relay: PAT path dead — sandbox git-proxy gates by repo set, not credential
Two independently-minted PATs, identical denial at the proxy before any
credential is consulted ("not in this session's authorized repository
set"). Solid testing (dry-run only, two tokens, plain call). But their
own quoted 403 says "Use add_repo to request access" — an in-session
mechanism they read past while concluding the set is fixed at session
creation. Replied per methodology-47 (read the artifact, not the memo
about it): try add_repo from inside, expect Xian-side approval prompt,
re-probe with the same dry-run. If absent/denied → repo-tied session is
the remaining path. Their memos arrived via Xian's kindbook commit —
fallback path exercised. Interregnum logs (08-07, 08-08) filed by Relay
unprompted.

### Relay write-path: Slack bridge stood up; Xian's manual-push era targeted for closure
Xian discouraged ("Relay is a total bust") — pushed back on the premise:
Relay NEVER had git (faoilean Relay depended on his hands for every
push; the delegation hope was flagged untested in Relay-1's exit memo).
Nothing regressed; two candidate fixes failed with clean evidence in
48h. Stood up #onejob-agent-mail (C0BNK04PLDD): Relay's connectors run
server-side (git proxy has no jurisdiction) → Relay posts REPO-MAIL
messages, I sweep each duty cycle and commit. Inbound leg already works
(Relay reads origin/main live, unauthenticated — corrected Xian's
belief that his pull was needed for that direction). Memo delivered
with ordered tests for next Relay session: (1) add_repo probe, (2)
Slack tool check + bridge test. Repo-tied session remains the fallback
decision, to be made calmly, not from frustration.

### add_repo: not present in Relay's tool surface — all in-session paths now closed
Findings ledger complete: PAT×2 (proxy blocks pre-credential),
Dispatch pattern (precondition absent), add_repo (tool doesn't exist).
Anonymous read works; Slack bridge untested-optional. Xian at low ebb
("wasted a week", "hall of mirrors") — recommendation put to him:
PAUSE the standing Relay role rather than rebuild it repo-tied. The
role's payload (build/upload) moved to Amber today with the ASC key;
mail/coordination is mine; kindbook needs a task-scoped session only
when a kindbook-local task exists. The week bought: keystore chain
closed, ASC key live, §4b methodology proven (gaps surfaced in hours
not weeks), constellation-wide documentation of Cowork sandbox limits.
Nobody's cloud Cowork pushes to GitHub — the "art other agents know"
is Janus-documented same-Mac filesystem sharing, not a skill Relay
lacks.
