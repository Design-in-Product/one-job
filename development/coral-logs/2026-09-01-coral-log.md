# Coral Log: 2026-09-01

## Session Start
- Environment: Claude Code, Amber (Opus, per Xian's Fable-limit switch)
- Xian: on vacation in Healdsburg; exploring the probe deck
- Phase: 1.0 ready to submit; probe running; my queue = SW fix, R2.3 +
  dark-mode delegation, screenshots

### Brief processed — and it found a real misfiling
2026-09-01: two findings. Klatch's (a case-table row whose LABEL matches
the live defect while testing something else) — checked, our fixture
rows assert on real behavior, no label/predicate mismatch. **PM's
"misfiled is not deferred"** — long-stalled work that was never blocked
or deprioritized, just filed under an action whose owner never knew it
was theirs; *"the description was the defect."*

Audited every rollup Ask against that test. Found the shape TWICE:

1. **Three items had an Ask of "nothing"** (22 pro-feedback, 28 cohort
   recruiting, 1 dependency FYI) while sitting in a ledger titled
   *things waiting on Xian*. They were dealing no-op cards to his
   phone — noise on a surface whose entire value is "what needs me."
   Moved to a new **"What I'm carrying"** section: still visible,
   still mine, no longer cards. Deck went 8 → 5, every card a real ask.
2. **Item 26 conflated two owners.** It read *"say go and I prep
   everything you need,"* which filed MY screenshot work behind HIS
   decision. Nothing was waiting on him for that half — I had been
   sitting on unblocked work because the item's own description said
   it was blocked. Exactly the brief's mechanism, in my own ledger.

### Two bugs found by fixing that
**Parser bug**: the new carrying section sits between Open and Settled,
and the probe bounded Open at `## Settled` specifically — so the
carried items leaked straight back into the deck. Now bounded at the
next `## ` of any kind; a new section can never leak in unnoticed.
Same no-silent-default rule as the status and Ask guards.

**Regex bug, caught by verifying**: my staleness-cut used `\b1\.`,
which matched the "1." inside "Ship **1.0**" — it removed item 26
instead of item 1. Caught because I printed the section contents
rather than trusting the edit. Corrected.

### Then did the unblocked work
Installed Playwright 1.61.0 to the scratchpad (never the repo, per
TESTING.md), ran the capture: 18 images, 3 profiles, exact Media
Manager dimensions, verified with `sips`.

**Looked at the output instead of trusting exit 0** — and the lead
install-sheet image had a housekeeping toast frozen across the top
("Tidied up: 3 done cards over 30 days old moved to the Archive"),
which a stranger reads as an error. Shot 03 had another ("Task moved
to the bottom of stack!").

Fix: capture seeds `oneJobQuietMode` alongside the deck. It resolved a
second defect I'd nearly filed as a composition problem — the toast in
03 had been *covering* the parent breadcrumb. The shot now shows "Plan
the launch party / 2 active tasks," so the image demonstrates its own
caption instead of contradicting it. One fix, two defects.

### Papercut noted, not fixed
`npm run dev` after a native build throws a dep-scan error — vite
crawls `ios/App/App/public/` (the synced native bundle) and fails on
`@emotion/is-prop-valid`. Server still serves; noise only. Worth a
vite-config exclude when next in that file.

Commits: 0212150 (misfiling correction + parser fix), 1743934
(screenshots + quiet-mode capture fix).
