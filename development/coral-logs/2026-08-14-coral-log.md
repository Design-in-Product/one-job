# Coral Log: 2026-08-14

## Session Start

### Context
- Environment: Claude Code, Amber
- Project: One Job
- Git Status: clean
- Current Phase: holding on Xian — device pass on rc.31 (PWA), one-time
  Distribution cert for the native export step. Both flagged 08-13,
  neither done yet as of this session start.

### Brief processed (via origin/main monitor, task b8b9ecwem)
`docs/briefs/cross-pollination/current.md`, 2026-08-14. Notable: my own
08-13 coral-log entry (first iOS archive succeeded, pipeline gap found
and scoped) got cited as a Sources Read example of m-49 applied in
practice — good visibility signal, no action needed from that.

1. **Mediajunkie/Terminus — a connector can return stale data even on
   a direct known-ID lookup.** Gmail thread fetch by exact ID, two
   methods, both ~18h stale, no error/staleness signal — silently
   missing the last three messages. Framing: "no new results" should
   be provisional, not confirmed absence, especially on fast-moving
   resources. **Relevance to One Job: moderate, filed for later.** Our
   GitHub import (R3.2, rc.21) polls "your open assigned issues" via a
   direct API call — the same staleness shape could apply (GitHub API
   returning a cached view on re-import). Not urgent since import is
   pro-walled/opt-in and re-import is user-initiated with visible
   results, but worth a mental note if we ever build automated/silent
   polling against it.
2. **Klatch — without an eviction notice, models make confident FALSE
   claims, not silent gaps.** A/B'd: gating a loss-notice on detected
   eviction fails because you can't detect what isn't there anymore;
   only an unconditional notice converts "confidently wrong" into
   "uncertain and asking." **Relevance: not to the app, but to me.**
   My own context gets compacted across long sessions (this one just
   was, per the conversation summary at top). If a constraint or fact
   got evicted in a compaction, I would have no evidence it ever
   existed — and would report confidently rather than provisionally.
   No code fix available (this is about my own operating practice, not
   the product), but worth carrying as a standing caution: after any
   context compaction, treat "I don't see mention of X" as "X may have
   been evicted," not "X was never true," especially for standing
   rules/restrictions rather than routine facts.

No action items generated for the product itself; both filed as
awareness for future work / my own practice.
