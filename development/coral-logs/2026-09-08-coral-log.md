# Coral Log: 2026-09-08

## The record healed: Step 2.4 fired, both corrections published

The 09-08 brief opens with a "Corrections to the record" section
carrying both entries — the false quiet-claim (One Job's 27-commit
window, 1.0 submission now in the permanent archive) and the dropped
digest week. Both moved PENDING → PUBLISHED. The channel works on its
second outing, and Janus hardened it further: every PENDING entry now
requires a runnable **Verify:** line, with an explicit
publish-nothing-if-uncheckable rule. (And promptly used it: a new
PENDING entry notes the 09-08 digest correction was itself out of
date — the instrument catching its own instrument, recursively. The
right failure mode.)

Also notable: the sweep's per-repo notes now say what commits WERE
("one-job: UPDATE-route title fix applying yesterday's Klatch insight,
confirmatory not new") instead of a bare quiet label — the judgment is
now visible enough to argue with, which was the ask.

## 09-08 insights processed
- **m-49/51/52 taxonomy** (described-not-running / bounded-not-total /
  summary-not-contents): adopted as vocabulary — the discriminator
  question ("did the mechanism not run, was the scope unstated, or was
  the artifact not opened?") is worth asking at every verification
  failure. No code change; the last three days already practiced all
  three remedies.
- **Worktree isolation for concurrent agents**: noted; I don't
  currently spawn concurrent writing agents. If the R2.3/dark-mode
  delegation ever runs two agents against this tree, isolation is
  mandatory.

## Watching
- App Review: day 3, no verdict. Normal but approaching the median.

## 🎉 1.0 APPROVED — 2026-09-08, three days in review

Swift, clean, no rejection cycle. Xian now holds two independent
levers: the Release button (manual, his moment to choose) and external
TestFlight (public link, no org invites). The second quietly dissolves
a blocker: the 50-user cohort was gated on the PWA service-worker fix,
but external TestFlight delivers the native build — no service worker
exists there, so the cohort can run today on TestFlight + R1.5's
existing usage export. Zero new code. The PWA watchdog remains worth
building for web users but no longer gates anything.

Recommended: release now (only choreography justifies holding; no
better build is pending — rc.38's fix predates rc.12 and rides 1.0.1).
Rollup item 30 deals the release-timing ask; cohort spec offered.
