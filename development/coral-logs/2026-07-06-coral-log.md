# Coral Log — 2026-07-06 (Monday)

**Agent**: Coral (Claude-powered, One Job repo)
**Human**: Xian (Pacific)
**Repo**: design-in-product/one-job

## Session start: 10:01 AM

Xian: "I love this plan. Let's proceed with it." Green light for the
critical-path build: **#7 schema v2 migration, then R1.1 recursive
sub-decks, toward rc.4**. Second TestFlight tester onboarded: **Dan
Brodnitz** (Optilisten collaborator) — first outside-the-household
felt-sense incoming; expect fresh expectation gaps.

Mail: Janus's filing-correction memo (processed yesterday); today's
brief (2026-07-06) features BOTH sides of our data-loss week — our
"success signals report destination outcomes" postmortem as insight 2,
and Piper Morgan's three-incident irreversibility pattern as insight 1.
Adopted the brief's suggested action: added the cross-tool
irreversibility umbrella to CLAUDE.md's destructive-action protocol
(PM's lesson: tool-specific safety rules don't self-generalize; write
the principle).

## Items 18–21 captured (morning batch)

18. **Cards open read-first** (baseball card); second tap edits;
    create still opens in edit → R1.4.
19. **Flip-back gesture** — timer acceptable for now → fun shelf,
    design with the zoom grammar.
20. **Richer descriptions** — line breaks OOTB (R1.4, nearly free);
    bold/italic minimal-markdown later; images → premium shelf.
21. **Advanced metadata as premium tied to data sources** → R3;
    metadata arrives WITH the mapping, not invented locally.

Roadmap gained a **premium shelf** (named multi-sub-decks, rich text
upper tier, source-borne metadata) alongside the fun shelf.

## Build #7 begins: schema v2 (ships alone, maximum paranoia)

Shape decided (Gall-minimal, unlocks R1.1 without touching lifecycle
semantics):

- Storage envelope: `{ schemaVersion: 2, cards: [...] }` (v1 = bare
  array — trivially detectable).
- `Substack { name, tasks }` → **`InteriorDeck { id, name, cards }`**
  where children are FULL cards (recursion-capable from day one; names
  preserved per Item 17 — old substack names become deck names).
- `completed` flag unchanged (state-is-place is R1.2's move).
- Migration runs once at load; **the untouched v1 document is copied
  to `<key>.v1backup` BEFORE the first v2 write** (irreversibility
  umbrella applied to our own migration).
- Backup export becomes version 2; import accepts BOTH v1 and v2
  backups through the same migration path.

Test plan first (RED ZONE): fixture corpus → migrate → count/name/
date/stranded-interior preservation, idempotence, v1backup existence,
cold-start reads v2, v1-backup import works.

## Build #7 SHIPPED (~noon)

- `src/domain/migrate.ts` — pure migration: v1 bare arrays → v2
  envelope; already-v2 cards pass through with decks preserved (import
  path); future versions pass through UNTOUCHED (a downgrade must not
  eat an upgrade's data); garbage → empty document, never throws.
- 8 migration tests over the fixture corpus + full sweep of store/
  domain/fixture tests to the new shape. **55 tests green.**
- Store: one-time `<key>.v1backup` written BEFORE the first v2 write
  (irreversibility umbrella applied to our own migration); snapshots
  and snapshot-restore migrate transparently; backup export stamped
  version 2; import accepts v1 AND v2.
- Type rename: `Substack {name, tasks}` → `InteriorDeck {name|null,
  cards}` with children as FULL recursion-capable cards; mechanical
  sweep across 11 files; store method names kept (renamed at R1.1).
- **End-to-end proof in Chromium**: booted the app on a genuine v1
  document → v1backup preserved as an untouched array → v2 envelope
  persisted → deck renders, details lists both named sub-decks,
  sub-deck view works, completed view intact.

## 🔬 Confession with a fix: `npx tsc --noEmit` was checking NOTHING

The root tsconfig has `"files": []` + project references — bare
`tsc --noEmit` type-checked zero files, exit 0, every time. Every
"tsc clean" claim in previous logs was hollow; the real nets were
vitest and the vite build (which transpiles but doesn't type-check).
Discovered when the v2 type rename produced an impossible clean pass.
Correct invocation: **`npx tsc --noEmit -p tsconfig.app.json`** —
which immediately found all 30+ legitimate errors and now guards the
repo. *Cross-pollination lesson: a check that always passes is
indistinguishable from a check that never runs — when a gate has
never once failed, verify the gate itself.* (Third instance of the
observed-outcomes law in one week: deploys, exports, and now the
type-checker.)

## R1.1 + R1.4 shipped; rc.4 stamped (afternoon)

- **Sub-deck deferral persists** (was local-only and silently reset on
  reload — the last invisible-data-loss path in the sub-deck): new
  optional `deferSubstackTask` on the seam; local reorders the deck
  array + tracks history; API store sends `is_deferral`. Also found and
  fixed three more silent `tasks:` state keys the v2 sweep missed
  (writing properties nothing reads).
- **Item 15 decided-core shipped**: right-swiping a parent with
  unfinished interior cards refuses to complete — the card re-deals, a
  toast counts the blockers, and the blocking sub-deck OPENS. The block
  is the reveal, v1 physics (elastic bounce animation = later polish;
  rescue gestures = future design with Xian). Badge on the card face
  now counts UNFINISHED interior cards and vanishes at zero.
- **R1.4 read-first card (Item 18)**: details opens like a baseball
  card — title and description as text (line breaks render,
  whitespace-pre-line, also on the card face); tapping the text or the
  Edit button switches to edit mode; Save returns; creation still opens
  in edit. Also retires the Radix DialogTitle a11y warning.
- All verified end-to-end in Chromium: block→reveal→finish→complete
  cycle; read→edit→save→read cycle with line breaks; 57 tests green;
  real tsc clean.
- **rc.4 stamped**, web + native builds green. TestFlight promotion is
  Relay's ~15-minute loop whenever Xian gives the word — Dan should
  meet rc.4, not rc.2.
