# Coral Log: 2026-08-13

## Session Start

### Context
- Environment: Claude Code, Amber
- Project: One Job
- Git Status: clean
- Current Phase: holding on Xian — rc.31 device pass, Pro-feature
  feedback, 1.0 build call (asked directly 08-12 19:11, offered to
  start with device pass or the quick caption/Media-Manager items)

### Brief processed (via origin/main monitor, task b8b9ecwem)
`docs/briefs/cross-pollination/current.md`, 2026-08-13:

1. **Piper Morgan — methodology-49, "Described Is Not Running."**
   Sharper corollary to the existing "describe ≠ mechanism" rule: an
   artifact that *accurately describes* a defect can become the next
   instance of that same defect, one level up (canonical case: docs
   quoting a Jekyll `{%...%}` parsing bug got parsed by the same Jekyll
   pipeline, killing the hub's Pages build for 2.5 months). **Directly
   reinforces the already-queued "Institutional Phantom" sweep**
   (08-11 brief, still pending) of CLAUDE.md/TESTING.md for "since/
   always/✅" claims — m-49 sharpens the standard: before writing "X is
   live/fixed/running" in a durable doc, the test is "did I watch X do
   the thing," not "did I describe X doing the thing." No new sweep
   scheduled beyond the one already queued; noting the reinforcement.
2. **Klatch — Claude models apply their own discretion on context
   provenance labels**, even after explicit user authorization to
   disclose in a shared room. Interesting emergent-norm finding; **not
   applicable to One Job** — we have no multi-agent shared-context
   injection surface. FYI only, no action.

No action items generated beyond the standing Institutional Phantom
queue.

### 19:11–20:xx — 1.0 decisions batch
Xian answered the store-submission unblock list:
1. **1.0 = rc.31, confirmed.** Rollup updated; final shutter still
   gated on his device pass (his own condition, restated).
2. **iPad: confirmed settled**, matches the 08-06 decision — no drift.
3. Bare file-path reference to the captions didn't render as a link in
   his client — built an interactive HTML artifact instead (caption
   approval w/ edit fields, Media Manager walkthrough, rc.31 device-pass
   routine): https://claude.ai/code/artifact/3db461ad-6668-4dcc-bb28-fa7d725cb5bf
4. He hasn't done the device pass yet — asked for a brief routine;
   folded into the same artifact as a 6-step ~10min workout instead of
   the full rc.30 checklist.
5. Pro-feedback fragments (unwritten, ahead of his fuller pass):
   inchworm-view opinions (unspecified), and a real gap — no deck
   delete or deck-into-deck merge (only top-level card→deck move exists,
   rc.18). Captured as rollup item 22, not yet scoped.

**Dependency audit, acted on rather than just explained:** re-ran
`npm audit` (my "18 open" note was stale — actually 20: 4 direct
[vite/postcss/uuid/react-router-dom], 16 transitive). Ran plain
`npm audit fix` (no `--force`) — all 16 turned out to be safe patch/
minor bumps, not the major-bump risk I'd assumed earlier. Verified:
build clean, 164/164 tests green, committed (89cdcb0). Remaining 4 (2
new react-router CVEs needing 6→7, vite needing 5→8/rolldown) genuinely
do need major bumps — held per the original recommendation, now for the
right remaining reason.

Reverted an incidental `demo.html` hash-rewrite from my local
verification build (sync-demo-assets side effect of `npm run build`) —
not something to commit; CI's own build produces the real pairing.

Rollup (docs/ATTENTION-ROLLUP.md) updated end to end: item 21 rewritten
down to two calls, item 1 (deps) rewritten with the fix, item 22 new
(pro-feedback capture), iPad noted settled inline.

**Known debt, not touched this pass:** rollup items 5/5a/5b (Relay
migration direction) are stale — Relay-on-kindbook is now paused with
honors per the 08-11 handoff, which predates and supersedes those three
entries. Left alone rather than scope-creeping this session; candidate
for the queued Institutional Phantom sweep or its own pass.

### 20:xx — follow-up round: risk-traced deps, applied captions, ASC walkthrough done
1. **Deck identity brand-ownership: kept at current default** (small
   wordmark, every deck). En route he floated a theoretical
   white-label/full-rebrand tier priced above Pro — not scoped, noted
   in PRICING.md for a future tiers pass.
2. **Dependency worst-case, traced not guessed:** pulled the actual
   advisory titles for the remaining 4. vite/esbuild/launch-editor are
   `devDependencies` — zero shipped exposure, narrowest real scenario
   is a hostile browser tab open during `npm run dev` reading
   dev-server-served source. react-router/-dom need attacker input
   reaching `<Link>`/`useNavigate`/SSR `deserializeErrors` — grepped
   `src/`: no `<Link>`, no `useNavigate`, no SSR anywhere, just one
   static catch-all route. **Practical worst case: none, today.**
   Recorded in the rollup with the actual reasoning, not just the
   verdict.
3. **Captions: applied, not just approved.** Xian rewrote 5 of 6
   directly in his own voice; edited `CAPTIONS` in
   `scripts/compose-store-shots.mjs` to match verbatim, kept 06 with
   his flag (may need rewording once accounts exist — noted inline).
   Also applied both LISTING.md PROPOSED deltas from 08-04 (substacks
   → smaller decks; review-notes menu list regenerated for rc.31's
   actual long-press menu, was still describing rc.12's). Did NOT run
   composition against the one on-disk staging set (2026-08-04) — it
   predates deck identity and everything since rc.13; would produce a
   misleading preview. Composing waits for the real shutter, against a
   fresh rc.31 capture, post device-pass.
4. **Media Manager: he did it inline**, pasting the live ASC page
   rather than following my walkthrough — worked fine either way.
   Found: Apple renamed "6.7"" to "6.9"" and added several smaller
   legacy buckets, but our three pixel sizes are already exact matches
   for the current buckets. No harness change; updated the stale
   comment in `capture-store-shots.mjs` so a future me doesn't get
   confused by the old label.
5. Rollup item 21 rewritten again — down to one real gate: his
   device-pass soak on rc.31. He confirmed he hasn't done it yet.

Commits this round: e8a0613 (captions + listing copy), 4f875fd (rollup
+ pricing). All pushed, verified on origin/main.

### 20:xx — asset-readiness check surfaces a real native-pipeline gap
Xian asked what else needs preparing before submission. Checked
end-to-end rather than assuming from the 08-08 runbook notes:

- Confirmed live/ready: privacy/support/marketing URLs (curled, all
  200), category + data-safety answers in LISTING.md, app icon asset
  present in Xcode's asset catalog, automatic signing configured with
  a team ID (`YZ4B34YGX9`) already in the project.
- `npm run build:native && npx cap sync ios` — clean, both Capacitor
  plugins (haptics, preferences) synced fine.
- **Real finding: no native archive has EVER been produced on Amber.**
  rc.12's TestFlight build was cut on the old laptop pre-migration. A
  first test archive failed immediately: the iOS platform SDK was
  never actually installed, only the macOS host tooling — the 08-08
  runbook staged the auth gate but that download step was never run.
  `xcodebuild -downloadPlatform iOS` in background, got silently
  killed (task notification said "stopped," no error) — what landed on
  disk was 145MB of stale iOS 15/16 device-support leftovers, not the
  real 26.5 platform. Retried properly with explicit `run_in_background`
  this time (task b4on8kpo7); disk has 183GB free, no space concern.

**Confusion surfaced and resolved**: Xian noted his TestFlight is still
rc.12 and asked directly "what am I supposed to be testing?" — my
device-pass artifact never named which surface. Clarified: TestFlight
(rc.12) is Dan's deliberately-frozen concept-validation build, separate
from 1.0; his signoff pass should be on the **PWA** at onejob.co/app,
which IS rc.31 and auto-updates. The native/App-Store artifact doesn't
exist as a testable build at all yet — that's the pipeline gap above,
mine to prove, not his to test. Per the 1.0 decision doc we were never
planning a second TestFlight round before submission anyway — his PWA
soak on the newer surface serves that role.

Nothing committed this round (research + a running background
download); will report back once the platform download completes and
I can retry the archive.

### 20:xx — platform download landed, archive succeeded, export found the real gate
Download completed clean: iOS 26.5 Simulator, 8.52 GB, exit 0 (task
b4on8kpo7). Retried the archive — **succeeded**, first one ever
produced on Amber. Automatic signing generated its own provisioning
profile via the ASC API key with zero interactive input
("Apple Development: Created via API (D96QY6RRB3)", "iOS Team
Provisioning Profile: co.onejob.deck").

Pushed further to prove the whole chain: wrote `ios/exportOptions.plist`
(method app-store-connect, team YZ4B34YGX9, automatic signing — the
runbook's own anticipated file, team ID now known from the pbxproj) and
ran `-exportArchive`. **Failed**: `Cloud signing permission error` /
`No signing certificate "iOS Distribution" found`. Real, distinct gap:
the API key's role (App Manager, chosen deliberately narrow back on
08-08) can't create a Distribution cert — Apple gates that to a higher
privilege tier. This is NOT something I can route around unattended;
needs Xian to either sign into Xcode's Accounts pane once (2FA) so the
normal account flow creates the cert, or make one manually via
developer.apple.com. Recorded in the rollup as item 21's new 🔴, not
blocking his device pass, only the final export/submit step.

`build/` (archive + failed export attempt, no .ipa produced) added to
.gitignore — was untracked, never staged. `ios/exportOptions.plist`
committed since it's real config the runbook always intended to check
in, now that the team ID is known.

Commits: 54e3d89 (exportOptions.plist + gitignore), d6baaf5 (rollup).
Net result of tonight's asset-readiness check: went from "assumed the
native pipeline was staged and ready" to "actually ran it, found two
real gaps, fixed one, clearly scoped the other for Xian." Exactly the
kind of thing worth finding now rather than at the actual signoff
moment.
