# Coral Log: 2026-08-26

## Session Start

### Context
- Environment: Claude Code, Amber
- Project: One Job
- Git Status: clean
- Current Phase: holding on Xian — white screen investigation, the
  Safari-tab-vs-home-screen-icon storage test, and the data-resilience
  writeup offer, all still open from 08-20.
- Note: hit a transient Bash-classifier outage at session start
  ("claude-sonnet-5 is temporarily unavailable"); retried once, cleared
  immediately. Same species as the 08-11 occurrence, no lasting effect.

### Brief processed (via origin/main monitor, task b8b9ecwem)
`docs/briefs/cross-pollination/current.md`, 2026-08-26:

1. **Piper Morgan — `gh issue list --limit N` silently truncates at N**;
   a full-repo audit needs `gh search issues` or `gh api --paginate`
   for a true count. Checked rather than assumed clear: this repo has
   exactly **1 issue total** (closed, confirmed via `gh search
   issues`), and grepped my own logs — no prior `gh issue list` usage
   here at all. Genuinely not applicable, by a wide margin, not just
   "probably fine."
2. **Klatch — a pre-registered prediction matching observed results
   doesn't confirm the mechanism actually ran**; check the precondition
   that proves it fired, not just the summary statistic. Self-check
   against our own practice: the deploy pipeline's `verify` job doesn't
   trust the GH Pages action's own "succeeded" status as proof of a
   live deploy — it independently curls the live site and compares the
   served bundle hash to what was actually built (the exact discipline
   this finding calls for, already in place since the 08-06 outage
   fix, and personally re-applied throughout the 08-13/08-16/08-20
   sessions by curling the live site myself rather than trusting CI
   green). Already-embodied practice, not a gap.

No action items — both findings checked against real counts/practice,
came back either clearly inapplicable or already correctly handled.

## Session #2 (6:42pm): VC-exercise memos, README fix, Dan's feedback, pro-feedback interview kickoff

Xian ran a Cowork research session today building an investor pitch
deck (MFM framework) against One Job, ran five parallel research
sweeps testing the venture case, got a provisional investment-
readiness review, then had that same process surface two memos filed
directly to `docs/mail/`: a roadmap-reordering recommendation and a
README statistical-corrections memo. Plus `docs/pitch/` now holds the
actual deck (.pptx) and investor reference (.docx). Plus he pasted
Dan's real testing feedback from a chat thread. Plus he wants to start
the conversational Pro-feedback interview (agreed 08-20) now.

**README memo — read, verified, acted on.** Two `README.md` lines
("23 minutes to refocus," "improves focus by 40%, reduces errors by
50%") traced by the Cowork research to no defensible primary source
(the actual CHI 2008 paper the 23-minute figure is usually attributed
to found the OPPOSITE direction — interrupted work took less time, not
more). Independently verified the memo's own scope-check claim before
trusting it — grepped every file it named plus src/ and docs/, found
the figures ONLY in the two README lines, confirming the memo's
research was actually exhaustive rather than assuming so. Applied
Option A (drop the numbers, keep the argument) per the memo's own
recommendation — this is clearly-right, low-risk documentation hygiene
with a rigorous paper trail behind it, so acted rather than asking
permission on something this unambiguous. Commit 74981b0.

**Roadmap-reordering memo — read in full, engaged honestly, not
rubber-stamped.** Core recommendation: R2 (spatial layer) holds in
favor of opt-in local instrumentation before inviting anyone, a
near-zero-cost "fleet probe" reusing ATTENTION-ROLLUP.md as literal One
Job cards fed to Xian's own agent fleet, one scoped read-only Todoist
integration, and recruiting 10 agent-heavy users before 40 ordinary
ones. Did NOT just agree — did the one thing I could actually verify
with code rather than take on faith: the memo's Question 2 asks
whether the white-screen bug can occur in the native/TestFlight build
"if that holds under one deliberate check." Checked `vite.config.ts`
directly: `mode !== 'capacitor' && VitePWA(...)` means the entire PWA
plugin (service worker, precache manifest) doesn't exist in the native
build at all, not just "hasn't triggered yet" — a structural
guarantee, not a probabilistic one. This upgrades the memo's
conditional §0 recommendation to a confirmed one. Also flagged
something the memo didn't cover: local instrumentation, even fully
on-device, needs a real check against Apple's data-safety
questionnaire before shipping (currently "Data Not Collected") — noted
as a real technical risk to verify, not assumed fine. And noted a
concrete near-zero-code implementation path for the fleet probe: it
can likely reuse the EXISTING v1/v2 backup-import mechanism
(Settings' "add as new deck" path) rather than needing anything new,
which matches the memo's own explicit "no adapter seam" ceiling
exactly. Logged as rollup item 25 — the big strategic calls (R2
holding, cohort order, pricing) are explicitly his to make; gave my
engineering read where invited, didn't presume to answer for him.
Commit 6fe33d2.

**Dan's feedback — real signal, cross-referenced against the memo.**
Notable: Dan's exact activation quote ("I just populated it right
away and I really liked that experience") already appears VERBATIM in
the roadmap memo — meaning the Cowork research already had access to
this conversation, so the memo's evidence base is more grounded than
it might have first appeared. Dan's core question — how does this fit
with his EXISTING to-do apps, is it a replacement or a decision layer
— independently and empirically validates exactly what the roadmap
memo argues for (R3/Todoist integration, "layer not replacement"
framing). Xian's own reply to Dan already stakes out real positions
worth deepening in the pro-feedback interview: the tension between
multi-deck utility and the "pure simplicity" of one card, and the
idea (echoing the Ted Nadeau call) of One Job as a unified front-end
across message sources, not just todo apps.

Xian wants the pro-feedback interview to start now. Natural first
question drawn directly from live material: the layer-vs-replacement
tension he articulated to Dan, and the multi-deck-vs-simplicity
tradeoff he named himself. Continuing in the same turn.
