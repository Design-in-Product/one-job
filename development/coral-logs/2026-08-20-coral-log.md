# Coral Log: 2026-08-20

## Session Start

### Context
- Environment: Claude Code, Amber
- Project: One Job
- Git Status: clean
- Current Phase: holding on Xian — device pass on rc.32 (the one real
  gate before store submission, per yesterday's catch-up), the small
  pro-backup-notice decision, and Pro-feature feedback whenever.

### Brief processed (via origin/main monitor, task b8b9ecwem)
`docs/briefs/cross-pollination/current.md`, 2026-08-20:

1. **Klatch — model reading a context range tends to stop at +6–10 rows
   from the offer start**, regardless of total offer width; "took the
   address" is a misleading success metric on its own. **Not
   applicable** — no retrieval-augmented multi-row context system here.
2. **Piper Morgan — a push chained unconditionally after a battery
   check runs even when the battery catches a failure**; fix is to
   decouple, read the exit code, THEN push as a separate step. **Checked
   against our own practice**, since this is a real risk pattern:
   `.github/workflows/deploy.yml` already structures this correctly —
   `test`, `build`, `deploy` are separate JOBS with dependency edges
   (deploy only runs if build/test succeeded), not a chained shell
   command where a later step runs regardless. My own local workflow
   (test/build/tsc, THEN commit, THEN push) already follows the same
   discipline in practice — verified this explicitly rather than just
   assuming, since it's exactly the kind of thing worth checking rather
   than pattern-matching "sounds like something I already do."

No action items — both findings checked against actual state rather
than skipped on assumption; nothing needed changing.

## Session #2: rc.32 device pass results — 2 of 5 clean, 2 real bugs, submission holds

Xian's report: warm-up, backup round trip, rooms/deck-switching, and
iPad all passed. Two real problems:

1. **White screen of death — happening on his phone RIGHT NOW as he
   reported it.** Treated as urgent immediately. Verified the live site
   independently before saying anything — curled every asset the live
   index.html references (JS bundle, CSS, manifest, sw.js), all 200.
   Confirmed genuinely not a server outage; his device is stuck on a
   stale service worker. Reassured him accurately: a render-crash
   doesn't touch localStorage, his data is very likely fully intact
   regardless of what the screen shows, and he already has a fresh,
   observed backup from today's own test anyway. Gave a staged,
   least-destructive-first recovery sequence (restart → force-quit/
   relaunch → open in Safari directly vs. the home-screen icon →
   TestFlight as an unaffected parallel surface) and was explicit:
   don't clear site data yet. Matches a failure class
   `pwaUpdateCheck.ts`'s own comment already predicted ("iOS home-screen
   PWAs only check for SW updates at launch... can sit on a stale
   version for days") — filed as needing a real fix (likely a
   version-mismatch detector / forced SW recovery path), not just today's
   workaround.
2. **Shake → confirm "undo" in the dialog fails** (menu-path Undo
   works). Read `Index.tsx` and `ActionSheet.tsx` end to end: both
   paths call the identical `handleUndoLast()`, the dialog's button
   wiring (stopPropagation on the inner sheet, direct onClick on the
   action button) looks structurally sound. No bug found by inspection.
   RED ZONE discipline (CLAUDE.md: task-state/undo logic needs tests-
   first, never guessed fixes) — did NOT invent a speculative fix.
   Asked him what "fails" actually looked like (silent no-op / error /
   crash) since that determines where to look next.

**Reframed the submission call, not just noted the bugs**: item 21's
"only real gate left: device pass" language was now stale and actively
misleading — the device pass RAN and found two real trust-surface
issues (a crash + a safety-feature failure), which is a materially
different state than "still waiting on the pass." Updated the rollup
to say so plainly rather than leaving stale text that undersold what
was found. Explicit tie to the standing R1 trust gate principle ("no
card lost, stranded, unrecoverable") — same discipline applied to
newer code, not a new bar.

Also from his message: (a) pro-backup-notice question — folded into
item 22 as agreed, merged the text rather than leaving a bare pointer
line; (b) Pro-feedback format decided — conversational, I ask per
feature/use-case, he answers, I log, we triage together. Not yet
scheduled; his call on timing. Rewrote item 22 to record the agreed
format and keep the 08-13 fragments as the seed for the first pass.

Commit: 7c6e2a9 (rollup — items 21/22/24 all touched). Did not touch
attention-briefing.html this round — the conversation itself carried
the urgent parts (white-screen troubleshooting needed to reach him
immediately, not wait for him to open the artifact); will fold into the
next refresh.
