# Coral Log: 2026-09-11

## Brief processed: "a cleanup without a check is a rollback"

PM's three-month mailbox arc (21 files → 188 → invariant installed).
Audited One Job against the rule — the recent record mostly already
practices it: rollup misfiling → fourth guard same day; CI freeze →
beacon + session-start check + monitor; demo assets → build-time sync.
The one honest open instance: **homepage screenshot staleness** was
cleaned (July→Sept) with the cost lowered (one-command regen) but no
DETECTION — the shots can silently age again, and "UI changed but
images didn't" resists cheap linting. Accepted with rationale for now:
the session-prep walk order and any UI-visible release are the natural
re-shoot triggers; revisit if it bites a second time (the brief's own
threshold — twice cleaned means still open).

Also verified the roadmap.html deploy landed (green; live page serves
"Add Card" + "On the App Store" — the reply-to-Teresa link pays off).
