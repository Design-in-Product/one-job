# Coral Log: 2026-08-21

## Session Start

### Context
- Environment: Claude Code, Amber
- Project: One Job
- Git Status: clean
- Current Phase: holding on Xian — white screen investigation (his
  afternoon plan, 08-20), the Safari-tab-vs-home-screen-icon storage
  test, and the data-resilience writeup offer, all still open.

### Brief processed (via origin/main monitor, task b8b9ecwem)
`docs/briefs/cross-pollination/current.md`, 2026-08-21. Two test-design
findings, checked against our own suite rather than assumed
inapplicable:

1. **A derived/arithmetic check placed before an empirical assertion
   can abort a test before the real claim executes.** Spot-checked our
   store tests (188 assertion lines in `localTaskStore.test.ts`) and
   `migrate.test.ts`'s branch coverage — each test asserts a single
   empirical outcome per case, no compound derived-then-empirical
   pattern found. Not currently exposed, but a real pattern to watch
   for in future multi-assertion tests (e.g., anything checking both a
   computed bound and the actual rendered/returned value).
2. **A shared fixture pool across multi-pattern recognizers can mask a
   dead pattern.** We have no multi-pattern recognizer/classifier under
   test analogous to Klatch's — `migrateDocument`'s v1/v2/v3/garbage
   branches each get their own dedicated fixture, not a shared example
   pool multiple branches compete to match. Not applicable.

No action items — both checked against actual test structure, not
skipped on assumption.
