# Coral Log — 2026-07-02

**Agent**: Coral (Claude-powered, One Job repo)
**Human**: Xian
**Repo**: design-in-product/one-job

## Summary

1.0 took shape: Xian set the direction (local-first, works for one person
on a phone, backend only for third-party sources), and the app became an
installable offline PWA with on-device storage, backup/restore, and its
first real-device feedback fixes. I was named Coral. App-store packaging
was scoped and greenlit as a parallel track.

## Work completed

1. **Local-first architecture**: TaskStore interface as the persistence
   seam — LocalTaskStore (localStorage, the default), DemoService (=
   LocalTaskStore + seeds + messages, isolated key), ApiTaskStore (only
   with VITE_API_URL or ?remote). Index.tsx fully rewired through
   getTaskStore(); substack tasks now persist in local AND remote modes.
2. **PWA**: vite-plugin-pwa (autoUpdate), manifest, card-medallion icons
   (rendered from an HTML source via Playwright), service worker. Verified:
   empty local start, persistence across reload, offline reload, demo
   isolation, remote round-trip.
3. **Settings + backup**: arc-menu Settings button (was a no-op) now opens
   a real view — storage-mode indicator, export to dated JSON, import with
   inline confirm. Verified export → wipe → import round-trip.
4. **Real-device feedback (Xian) fixed same-day**: (a) vertical drift —
   100vh counts the collapsed mobile URL bar; switched shell to 100dvh with
   vh fallback; (b) duplicate card-back flash on mount — entering card
   cross-faded over the static underlay; AnimatePresence initial={false}.
5. **Janus memo** filed at development/memos/ (GitHub scope limited to this
   repo; Xian to relay or widen scope later).
6. **Decisions/answers**: app is already quietly usable by anyone at
   /app/ (private per-device instances; discoverability deliberately
   deferred); app-store analysis — Android TWA ≈ a day, Capacitor iOS+
   Android ≈ 2–4 days code + process tax; Apple 4.2 risk low (native
   gestures, offline, no login), haptics will help.
7. **Process**: daily coral logs established at development/coral-logs/
   (this file); yesterday's reconstructed. Briefing-format alignment
   pending — designinproduct.com/internal is blocked from this sandbox.

## Direction set by Xian

- 1.0 = him, on his phone, personal tasks, starting today. Real feedback
  incoming.
- App-store releases: greenlit as parallel track. He has an Apple developer
  account (+ Mac, Codex/Cowork agents for local steps); Android account
  needs setup. Keep the app quietly usable meanwhile.

## Innovations worth cross-pollinating

- **Demo mode as a costume over a local store**: generalizing the demo
  service into the default persistence layer made "local-first" a one-day
  pivot instead of a rewrite — and dissolved hosting-cost and auth
  problems for 1.0 entirely.
- **Icon/banner pipeline**: brand assets rendered from self-contained HTML
  via headless-browser screenshot; deterministic, versioned, regenerable.
- **100dvh app shells**: any mobile web UI centering against 100vh will
  drift; dvh-with-vh-fallback utility pattern.

## In progress at log time

- Capacitor foundation for app-store builds (native build mode, storage
  durability bridge, platform scaffolds, runbook for Mac-side steps).

## App-store track (afternoon)

- Coral logs established (this system); 07-01 reconstructed; CLAUDE.md
  session protocol updated. designinproduct.com/internal blocked from
  sandbox — format alignment pending.
- Capacitor foundation shipped: `npm run build:native` (relative base,
  hash routing, no SW) → dist-native; android/ + ios/ projects generated
  and synced; storage durability bridge (localStorage mirrored to
  Capacitor Preferences, hydrated pre-render on native); viewport-fit=
  cover for safe areas; NATIVE.md runbook for Mac/Cowork/Play-Console
  steps. Verified native bundle boots + persists at a nested path
  (file:// blocks module scripts — WebView schemes don't; test via
  static server instead).
- Xian: Apple dev account yes, Android account to set up, Mac steps via
  Cowork agents; starts using the app for personal tasks today.

## App-store track, part 2 (advanced to the human-blocked line)

- Full native icon/splash sets generated into both scaffolds from
  browser-rendered masters (resources/ + scripts/ sources); sharp/
  @capacitor/assets blocked by sandbox proxy → canvas pipeline instead
- Haptics on swipe commit (native-only, no-op web)
- Android CI: manual-trigger workflow builds sideloadable debug APK
- privacy.html (Data Not Collected) deployed via Pages workflow
- store/LISTING.md drafted; store screenshots generated at 6.7"/5.5"/
  Android sizes with believable seeded tasks
- Remaining blockers are all human-side: bundle-id confirmation, Apple
  signing on Mac, Play Console account, keystore custody, copy approval
