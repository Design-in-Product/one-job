# Coral Log: 2026-08-24

## Session Start

### Context
- Environment: Claude Code, Amber
- Project: One Job
- Git Status: clean
- Current Phase: holding on Xian — white screen investigation, the
  Safari-tab-vs-home-screen-icon storage test, and the data-resilience
  writeup offer, all still open from 08-20.

### Brief processed (via origin/main monitor, task b8b9ecwem)
`docs/briefs/cross-pollination/current.md`, 2026-08-24:

1. **NYT Crossword/Xian — a deterministic pipeline surfaces unhandled
   input as a diagnosable failure; an adaptive layer absorbs it
   silently. Prefer deterministic, own the catalog of shapes.**
   Checked our own code against this rather than just agreeing in the
   abstract:
   - `migrateDocument()` already embodies this exactly — the 08-04
     near-wipe fix replaced silent "unrecognized shape → treat as
     empty" behavior with an explicit `FutureDataError` refuse-to-boot
     for any versioned-but-unrecognized envelope. This IS the
     deterministic-over-adaptive choice, already made, already the
     reason that fix exists.
   - Checked `githubAdapter.ts` too: its `try/catch` blocks all guard
     `localStorage` access failures (private-browsing edge cases), not
     malformed API data — a bad HTTP response throws a real `Error`
     with the status code, not a silent fallback. Confirmed the same
     principle holds there.
   Both already correct; nothing to change, but a real check against
   two live code paths rather than a symbolic nod.
2. **Klatch — an unquantified "plausible but uncounted" claim can run
   opposite to intuition when actually measured.** Self-check: the
   dependency-vulnerability "practical worst case: none" claim from
   08-13 was already an actually-measured claim at the time (grepped
   for `<Link>`/`useNavigate` usage, confirmed zero matches, not a
   hand-wave) — already the right discipline, nothing to redo.

No action items — both findings checked against real code/past claims
rather than accepted on faith; both came back confirming existing
practice rather than surfacing a gap.
