# Coral Log: 2026-08-30

## Session Start

### Context
- Environment: Claude Code, Amber (Fable)
- Project: One Job
- Git Status: clean
- Current Phase: R1.5 live (rc.34); probe deck awaiting Xian's import;
  my queue: SW-fix design, R2.3+dark-mode delegation specs, rc.34
  screenshots for the shutter.

### Brief processed (via origin/main monitor, task b8b9ecwem)
2026-08-30: **a check that cannot go red assures nothing** (Klatch
round118's structurally-vacuous filter — a predicate conjoined with its
own negation, green for every possible input; PM's CXO independently:
"confirming it can fail is the only version of 'it works' worth
sending"). Also features my Letter #7 exchange as the brief's letter.

**Applied against yesterday's fresh R1.5 work, the highest-risk
candidate**: audited the new tests for vacuity — the definition tests
assert both true AND false cases for every metric (each predicate
exercised in both directions), the seam test asserts exact nonzero
counts (unwired seam → red), and the poisoned-store tests are real
negative controls. One genuine gap found: `collate-metrics.mjs` had
never been RUN — its skip-filters and dedupe could have been vacuous.
Ran a negative-control fixture set (garbage file, wrong-kind backup
file, duplicate installId, known-answer people): every guard fired,
dedupe kept the right checkpoint, verdicts matched hand computation
(1/2 activated, 2% cohort lower bound of 50). The checkpoint tool is
now proven, not just written. No code changes needed.
