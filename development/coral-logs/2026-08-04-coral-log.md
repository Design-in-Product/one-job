# Session Log: 2026-08-04

## Morning (Xian + Relay working on faoilean; Coral on Amber)

### The push-script recursion (brief-worthy, maybe the best specimen yet)
relay-push.sh v1 committed the exact sin it exists to prevent: its
"✅ delivered" check (HEAD ancestor of origin/main?) is vacuously true
when nothing was staged — so it reported delivery while the changes,
INCLUDING ITS OWN BUG FIX (scripts/ was outside its safety scope), sat
uncommitted. Caught live this morning when Xian's git-status paste
showed the modified files; I flagged that the script can't deliver
itself; Relay rewrote v2 (stages scripts/, takes extra paths, and
LISTS any tracked-but-uncommitted file with a plain NOT-delivered
statement). Their header line for the family album: "A success signal
that cannot observe the thing it claims is worse than no signal,
because it actively stops you looking." For Janus: the
delivery-verification tool itself needing delivery verification closes
the loop on this whole arc (07-25 undelivered memo → 08-01 rule →
08-03 script → 08-04 the script's own sin).

### Etymology recorded (Xian): One Job ← "you had one job"
The name is a joke the reader is in on; copy plays to it rather than
explaining. Caption 1 ("You have one job.") closes the loop the name
opens. Now in LISTING.md as a standing copy principle.

### Migration plan drafted (the due promise)
Xian asked if anything blocks requested work — audit found the one
item that had silently come DUE: the Relay migration plan (promised
"after the device pass"; pass landed 08-03). Drafted: the unbundling
insight is the core — "Relay" was three unlike jobs (git it couldn't
do, Xcode GUI, hands-on-device), and the migration sends each where it
belongs (code agents / Amber unattended / kindbook successor). Steps
sequenced behind the two auth gates Xian already holds. One new
question: keep the name Relay? Lean yes. Conventions explicitly do not
migrate — they're what survived three machines.

### Janus status signal + credit correction
Xian's OpenAI verification: started, under review (Janus relay; nothing
on our track blocks on it — plan-for-approval). Corrected the ledger by
memo, delivered+verified: relay-push.sh is RELAY's build (I stated the
rule; they automated it, then owned and fixed v1's own violation).
This week's briefs are literally about corrections chasing every
surface a claim reached — practicing it.

---
## INCIDENT (Aug 4, midday): stale-SW rc.8 read v3 data as empty ("wipe")
Xian: onejob.co/app/?pro=comp "loaded rc8 and wipes my data." Facts
established before acting: **server is serving rc.22** (read the
deployed bundle) — so rc.8 was HIS DEVICE's stale service worker (a
browser last opened mid-July booting its cached build first; the SW
updates in background, stale page paints first).

**Mechanism:** rc.8's migrateDocument, reading a v3 envelope
({schemaVersion:3, decks:[...]}), fell through its shape checks to
`return {schemaVersion:2, cards:[]}` — EMPTY, not refusal. The wipe was
almost certainly VIRTUAL: rc.8 doesn't write the main key on load, and
its snapshot writer carries the empty-over-non-empty guard. Steps to
Xian: touch nothing → full close/reopen (activates rc.22) → expect the
deck intact; layered recovery (7 snapshots, .v2backup, his file) if not.

**My accountability:** the v3 migration (rc.16, mine) created the
forward hazard — older-code-x-newer-data returns empty instead of
refusing. Same class of hole existed one version forward in rc.22
(v4-shaped data → wrapAsRoot([])).

**Shipped (rc.23), red-first:**
1. `FutureDataError`: a versioned envelope whose container this build
   can't read now THROWS ("written by a NEWER version… your data is
   intact") — never returns empty. 3 domain tests.
2. The store RETHROWS it instead of quarantine+snapshot-restore — which
   would have rolled an OLDER snapshot over newer data and saved it,
   converting loud refusal into silent loss. Store test pins: main key
   byte-identical, no quarantine minted, constructor throws.
148/148. Old clients (rc.8-era caches) can't be retro-patched — their
protection remains the no-write-on-load behavior that held today.

### Incident RESOLVED: zero loss — and the fix he predicted, shipped
Root cause final: the ?pro=comp link opened in Chrome-the-browser, a
SEPARATE iOS storage container from his daily home-screen PWA. Chrome's
container: last visit mid-July → rc.8 service worker + July-era storage
("Boop" = his own old test card, a fossil, not a wipe artifact). His
real deck: intact in the home-screen container the whole time; fresh
backup made. Total loss ZERO. His "different account" instinct was the
right shape, wrong boundary — containers, not accounts.

**rc.24: in-app pro code.** His 07-30 prediction ("needs something
that can work in the app") became today's requirement: installed PWAs
have no address bar, and grant-URLs route through whatever browser owns
links — possibly the wrong container entirely. Settings → Pro features
→ "Have a code?" → `comp`. Wrong codes refused with a toast; Remove
reverses; same hasPro() seam, write path still pre-business-simple.
E2E: no-Decks → wrong code refused → grant persists → Decks… appears
same session → remove reverts. 148/148.

Incident ledger: one real bug found+fixed (rc.23 future-data refusal +
no-rollback), one UX hole closed (rc.24), one recovery playbook
exercised calmly under the protocol, zero data lost.
