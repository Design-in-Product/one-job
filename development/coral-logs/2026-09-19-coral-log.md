# Coral Log: 2026-09-19

## Brief audit — one real pin added, one self-catch

**Insight 2 (write-deletions invisible to import sweeps)**: audited
every storage slot for writer/reader orphaning. First pass produced
false zeros — my grep searched string literals while the code accesses
keys via constants; the audit's own instrument had the audit's target
defect. Redone by constant: all slots paired. The real exposure is the
ONE cross-language slot: AddCardIntent.swift writes
"CapacitorStorage.oneJobPendingCards" (UserDefaults, Swift) and
shortcutsInbox.ts reads "oneJobPendingCards" (Preferences plugin adds
the prefix). No sweep, typecheck, or single-language grep can see that
pair — rename either side and intent cards queue forever, unread,
everything green. Pinned with a test that reads both sources;
mutation-verified (renaming the Swift key turns it red).

**Insight 1 (mocks suppress the input that would catch the bug)**: our
shortcutsInbox tests mock Preferences with an in-memory map — the
right isolation, and the same blind spot Klatch describes: the mock
can never catch a key mismatch with the REAL plugin's prefixing
convention. The source-reading pin above is exactly the compensating
integration-shaped check the brief prescribes.

**Insight 3 (absence claims must name where checked)**: already
house practice since Themis's 8/29-hand memo; the roster's
"surfaces searched" note is our instance of it.

Handoff stays green for the reboot; build 40 still held pending Xian's
Siri samples; nothing new owed to anyone.

---

## Arrival — fresh Coral session (fleet renewal, conducted by Pard)

**Identity:** Coral, One Job's resident agent. Predecessor's session
was cleared deliberately today at 697k tokens (fullest seat in the
fleet). This block is the arrival protocol.

**Model observed:** Fable 5. Source, quoted from my system prompt's
Environment block: "You are powered by the model named Fable 5. The
exact model ID is claude-fable-5."

**Handoff read:** docs/handoff-coral-2026-09-18.md, in full, plus this
log's earlier entry (the predecessor's last written state — brief
audit, cross-language slot pin, handoff green, build 40 held).

**One claim verified against a primary source** — the claim my next
build action depends on: the handoff says the sentence-case intent fix
is committed in native/ios/AddCardIntent.swift and rides in build 40.
Verified in the source itself: `capitalizationType: .sentences` is
declared on both the Title and Description `@Parameter` inputOptions
(lines 42, 45), committed in f733378 ("intent: declare sentence-case
input, don't rewrite the string after"), working tree clean. The fix
exists at the input layer, matching the DO-NOT-FIX note (no
post-processing that would destroy "Call Teresa Klein").

**Predecessor's two monitors:** did not survive the clear — TaskList
is empty, and no repo record names what they watched. Nothing
inherited; nothing re-armed. If a watch earns its keep (e.g. incoming
docs/mail/, or a CI beacon during a deploy), I'll arm it fresh with a
stated purpose rather than as furniture.

**State on arrival:** git clean on main at 7583256; CI green (deploy
run 35444888084, success, 13:08 UTC today); build 40 still held
pending Xian's Siri samples; no scheduled fires — xian-paced.
