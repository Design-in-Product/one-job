# Session Log: 2026-08-06

## Session Start: 9:45 AM (Xian)

### Context
- Model: **Fable restored** (Opus 5 interlude closed).
- `main` at rc.25, clean, in sync. rc.25's two fixes live on web.
- No Pard→Coral config message yet — coming with the reboot per Xian.
- Xian's agenda today: (1) update the working artifact
  (attention / decisions / tests), (2) complete the Relay handoff,
  (3) prep config correction + Amber reboot with Pard.

### Session Objectives
1. Rebuild briefing artifact to current three-band shape
2. Draft the successor-Relay brief (migration plan step 1 — mine)
3. Config-correction prep: document the MEMORY-directory migration
   (my persistent memory lives under ~/.claude-kindsys/projects/…;
   the config move must carry it or I lose recall silently)
4. Support Xian's review + next steps as he re-orients

### Morning yield
1. **Artifact rebuilt to Xian's three-band framing**: (1) needs
   attention — keystore 🔴 final step, reboot+config prep (with the
   memory-migration warning), Relay-handoff remainder; (2) decisions —
   1.0 build, successor name, captions, iPad, deck identity (his
   finding), search visibility (his finding); (3) still to test —
   RETEST of his two bug fixes on rc.25, rooms search now that he
   knows where it lives. Div-balance sanity-checked before publish.
2. **Successor brief written** (store/COWORK-SUCCESSOR-BRIEF.md) — the
   migration plan's step 1, mine. Read order: this → RELAY-HANDOFF →
   COWORK-IOS-BRIEF. Core changes front-loaded: delegate ALL git to
   code agents (Xian's hard requirement), Amber takes builds when the
   ASC gate opens, the device-in-hand duties are the irreducible core.
   Predecessor's four paid-for laws distilled with pointers to
   receipts. First-session checklist ends with a boot memo,
   delivery-verified.
3. Config-correction prep: the memory-directory migration
   (~/.claude-kindsys/projects/…/memory/ must move with me) is now in
   the artifact, this log, and Pard's mailbox via yesterday's memo —
   three surfaces, per correction-propagation discipline.

### Deck-identity exploration delivered (his assignment)
Four directions rendered as phone-scale comparisons, each with the
**14-px peek test** — the constraint that decides most of it: identity
that dies at sliver width only helps after you've arrived. A name-only
(brand-pure, text wayfinding) · B hue-per-deck ★ (the literal playing-
card red-deck/blue-deck move; deck-1 keeps brand; peeks become
identifiable) · C pattern-per-deck (handsome, fails at 14px) ·
D monogram medallion (strongest, but spends the "1"). Rec: B + A's
name chip; hue as DECK DATA not device preference (same look on every
device and in every backup); rooms keep their gray — system places
never take user hues. Doc + images committed; his proposals next.

### Also this block
- Keystore: backed up per Xian (asked once for the open-to-verify
  confirmation; #18 closes on his yes).
- Memory: manifest + tarball done (~/coral-memory-backup-2026-08-06);
  repo is PUBLIC so his memory-in-repo idea was correctly rejected —
  checksums verify bit-for-bit without publication. RE-EXTRUSION at
  cutover agreed.
- rc.26: entitlement grant-date diagnostic for his repeating-comp-code
  report (same-container grant loss, cause unknown — now instrumented).
- Sequencing agreed: keystore → Relay stand-down → reboot/config.
- Undo semantics answered (multi-step, no redo — offered as decision).
- Riffle/reorder confirmed deliberately-unbuilt (R2.8 gate = his sift
  verdict). Search threshold: settled, no change wanted.

### Deck identity round 2 — his notes → synthesis rendered
His three constraints landed: name-first (D's placement), B overall
(with the brand-ownership question opened — how much "One Job" belongs
on a pro feature's backs at all), and the NEW surface: deck identity on
the FACE-UP card. Rendered E-synthesis.png: synthesized back (hue owns
deck, name at top, brand as quiet wordmark) + three face-up options
(edge band+eyebrow / corner chip / hued margin). My lean: hued margin —
the back's cream-margin idea, same material language, zero new chrome
in the reading surface. Keystore verify recipe sent (checksum compare —
bit-identical beats keytool-list for backup validity).

### Deck identity SHIPPED (rc.27) — design session → build, same day
His blessing with tuning params ("margin best, ~50% saturated, maybe
thinner") became the build: palette in domain (6 hues, brand-range;
deck-1 = undefined = brand forever; first-free assignment, cycle on
exhaustion — 4 tests red-first), color as DECK DATA (schema-additive
field, no migration), CardBack name-first layout (name top, wordmark
quiet; classic layout when no identity → single-deck users unchanged),
TaskCard hued margin (4px, color-mix 50% toward warm ground) + eyebrow,
CanvasPeek inherits hue — the 14px sliver becomes wayfinding. Index
gates identity on deckCount>1. E2E: ocean back name-first, face margin
+ eyebrow, brand deck-1 with name, colored peek, live createDeck →
forest assigned in storage. 154/154. One probe lesson: text-transform:
uppercase changes innerText — case-blind assertions or evaluate
textContent. deck-1 in multi-deck mode DOES take the name-first layout
(you need to know where you are everywhere) while keeping the brand
gradient — the distinction is color vs layout.

### Afternoon: his test batch → rc.28 + two briefs
Composer fixed (my rebuild removed decide-notes; unguarded lookup
killed ALL script — guards added). rc.28: redo (4 tests red-first; new
action ends the redo line), undo/redo return FACE-UP (his expectation),
rename-deck UI (store API's first surface) + plain-english defaults
("first deck", "deck 2"), shake diagnostic breadcrumb in Settings.
E2E visual (innerText lies — both faces in DOM, again). Captions
APPROVED recorded. 1.0 factors now a FILE
(plans/2026-08-06-release-1.0-decision.md) — the frame: two audiences;
inventory rc13-28 with lobby tiers; rec = current tag once HIS rc.28
device pass settles; floor rc.23. iPad alternatives rendered (a/b/c)
answering "images or functionality?" — both. GitHub wrong-repo report:
working-as-built (ALL assigned issues, all repos — hence PM backlog);
intent gap filed → repo-scoping as next adapter iteration. Rooms
"only one card": sift gesture undiscovered — answer given; hint
visibility noted as design question. Shake "not at all now": cause
unknown; Settings now shows listener state — next report carries
evidence.

### Evening: the deploy was stuck, sift was dead, and both are fixed
1. **"Phone won't update past rc.25" — it was the SERVER.** onejob.co
   was serving rc.25: a stuck GitHub-internal pages run wedged the
   shared `pages` concurrency group; my rapid pushes kept superseding
   queued runs (cancelled, not failed — build/deploy skipped). Cleared
   the zombies; rc.29 push carries 26–28 out. Lesson: verify deploy
   completion after push BURSTS, not just per-push. My own "it updates
   on open" claims for 26–28 were wrong — the deploy-verify habit
   lapsed exactly when the push rate rose.
2. **Sift dead since rc.20 — MY regression, his report found it.** The
   rooms pan wrapper used stopPropagation in the CAPTURE phase to
   "protect" card swipes — which fires BEFORE the card and so blocked
   every pointerdown from reaching it. A guard that fires before the
   thing it guards is a blockade. Post-pan probes never re-swiped a
   room card (they dragged backgrounds), so the harness missed it.
   Fixed with the strip's own pattern (dragListener=false + controls
   armed from background-only pointerdown). E2E now: sift walks, pan
   preserved.
3. **iPad DECIDED (a)**: "a pack of cards is a pack of cards" — no
   scaling; the big canvas's destiny is more decks, not bigger cards.
4. **Feature stages framework doc** (his 4 tiers → registry + gates;
   githubImport proposed → beta) + **GitHub v2 spec** recorded (pick a
   repo/board; conventional labels for round-trip; his token WOULD have
   bounded v1 if the endpoint weren't repo-agnostic — both true).
5. Release doc addendum: free/pro table at 1.0; no public pro door at
   launch (deliberate — pro is the paid rollout's launch supply);
   drumbeat rhythm recorded.

### The deploy mystery's REAL root cause: GitHub major outage
Deeper than the wedge theory: rc.29's push minted NO workflow run at
all → checked githubstatus: **Actions AND Pages in major_outage**. The
whole day's deploy weirdness (stuck internal run, cancelled cascade,
absent runs) was upstream. Corrections discipline: my earlier "cleared
the wedge" claim was a partial cause at best — the outage is the story.
Added workflow_dispatch to deploy.yml (pushes made mid-outage never
retro-trigger; recovery needs a hand-crank — also plain good hygiene).
Recovery monitor armed: watches githubstatus every 5 min, tells me to
dispatch when Actions+Pages go operational → rc.29 (carrying 26–28)
ships the moment GitHub breathes again.

### Night: rc.30 + Relay stand-down sent + artifact per-surface tests
rc.30: feature stages LIVE (registry+gate; beta = pro + Settings
toggle, alpha substitutes for toggle never for pro — the submitted
build carries no beta surface by default; inchworm + githubImport
demoted per his two calls) · select-none shell (his long-press
text-selection peeve; TaskDetails stays selectable) · PRICING gains
the IAP-compliance answer (sold codes outside the app = Apple's 3.1.1
evasion screen; comp-only at 1.0; the $1.99-2.99 probe = proper
StoreKit later; hasPro() seam survives). 163/163.
Relay formal stand-down memo DELIVERED (verified). Artifact test band
rebuilt per-surface (phone rc.30 / iPad / TestFlight-nothing) with
explicit WAIT-for-deploy note. Deck identity: his fence noted — ships
pro-walled, iterate after his test; no action.

### Relay's exit memo — four gaps, one critical, all actioned
Their flag with teeth: the successor brief's HARD REQUIREMENT (code
agents do git) rests on a capability Relay TESTED AND LACKED — spawned
agents inherit the parent's credential-less sandbox; kindbook is
unverified. And the failure is SILENT (a successor quietly using
Xian's hands looks like a working successor). Brief amended: §4b check
is now mandatory, inside a spawned agent, BEFORE the boot memo, with
loud-failure instructions. Also: keystore sha256 now published in the
handoff (my "checksum-verified" claim wasn't re-verifiable by anyone
later — they were right to call it); the OLD-DELETE-ME signing-key
copy became Xian's explicit deletion item; their handoff refreshed
RESOLVED-with-reasoning-preserved. Final ack delivered. A session that
closed by reviewing the plan for its own replacement — the culture
holding all the way to the last commit.

### Outage recovery: rc.31 verified live — and the hand-crank was a green no-op
GitHub recovered ~23:15 UTC. First firing of the workflow_dispatch
hand-crank exposed its own silent-success bug: deploy/verify were
guarded `if: github.event_name == 'push'` (meant "not PRs", said
"push only"), so the dispatch ran test+build and SKIPPED deploy —
green run, nothing shipped. Caught because the live bundle was
re-checked outside the run rather than trusting "RUN GREEN". Guard
fixed to `!= 'pull_request'`, annotated in the workflow. Webhooks
returned mid-fix; the push itself minted the run that shipped
rc.26–31. Verified by observation: live bundle index-Bs6C3Vke.js
contains stamp 1.0.0-rc.31 (hash differs from local build because CI
bakes VITE_API_URL — the verify job's own-build comparison is the
authoritative one). Xian pinged via Slack DM (path tested earlier at
his request — he's out for ~an hour). Also cancelled a YEAR-old
queued pages-build-deployment fossil (July 2025) — same shape as his
remembered Piper Morgan stuck-workflow incident, harmless here but
now unambiguous. Recovery monitor stood down; origin/main watch
stays armed for the successor's boot memo. Fourth member of the
silent-success family this project has caught: export toast,
relay-push v1, quiet-mode answers, and now the dispatch guard.
