# Session Log: 2026-07-29

## Session Start: 5:53 AM (Xian, remote)

### Context
- Project: One Job, `main` at 1878b2c, clean, in sync with origin
- First full day on Amber; kindsys account window closes ~tomorrow
- Model: restored to Fable by Xian (had silently dropped to Opus at
  some point yesterday — worth remembering that this can happen
  mid-session without announcement)
- No overnight mail; no new cross-pollination brief yet at this hour
- rc.12 verified live at onejob.co/app by observation (curled the
  deployed bundle, grepped the stamp) — Xian can test via his installed
  PWA remotely; Amber's dev server not needed since nothing is unpushed

### Session Objectives
1. Get Xian the attention rollup remotely → done: published
   docs/ATTENTION-ROLLUP.md as an artifact (repo file = single source
   of truth; republishing keeps the same URL as items settle)
2. Answers to rollup items as they arrive; unblock accordingly
3. Whatever the day brings — beta week is the main thread

### Session Activities
- 05:53 — synced, no changes overnight. Published rollup artifact.
  Verified deployed version is rc.12 before telling Xian to test via
  the PWA rather than assert it from CI green.

### Xian's design answers → shipped the same morning
All three questions from yesterday's options doc answered, plus a
product brief (fussiness principle, funnel goals, aesthetic). Decision
record appended to DESIGN-OPTIONS-2026-07-28.md; GESTURES.md updated.

**Shipped, RED-zone first (8 tests written failing, then green; 101 → 
via tsc clean; all Playwright-proven at 390×844):**
- **Trash is not protected**: swipe-right in Trash = one-tap permanent
  delete (the chain grammar's "right advances" reaching its natural
  end); Empty-trash button with the room's only remaining confirm
  (bulk); `withoutTrashed` domain helper + backups exclude trash;
  covenant 7 in trash — no tab count, no "n of m", felt pile of capped
  edge bars instead. Done keeps its count (trophy).
- **Rooms search** (title + description, his call #1): a filter on the
  pile from 6 cards up — narrows what the sift walks, no results list,
  one-card-at-a-time intact. Proven: query "dentist" matched one card
  by title and another by description only.
- **Undo**: session-deep history captured at the store's saveTasks
  choke point — zero per-method wiring, undoes ANY mutation including
  whole-deck import (tested). Suppression flag so undo's own save isn't
  captured (regression test: walks history, not a two-state toggle;
  wrote the test before the code). Menu: Undo leads the hold-menu.
  Shake: devicemotion hook with iOS permission piggybacked on first
  tap; prompts (a shake can be an accident) and only when history
  exists. E2E: complete-by-swipe then menu-Undo un-completed the card.
- Purge copy honesty note + two new rollup questions (housekeeping N
  days; shake needs real-iPhone verification) filed.

**Stamped rc.13** immediately — same-day application of yesterday's
lesson (main must never sit ahead of the memo'd stamp). Relay memo is
explicitly a non-superseding one: beta week stays on rc.12; git tags
now mark stamps so cut targets are unambiguous.

### Push race, tag repair, brief audit
- My rc.13 commit raced Janus's brief delivery (dc02acc); rebased clean.
  Trap caught: the rejected branch-push had still pushed the TAGS, so
  the remote rc.13 tag briefly pointed at the pre-rebase orphan.
  Force-updated that one tag to the rebased commit and verified all
  three refs by ls-remote. Tags now mark every stamp (v1.0.0-rc.12 →
  97ca4ae, v1.0.0-rc.13 → 8e64efe) so Relay's cut targets are commits,
  not "fresh pull of main."
- 07-29 brief read. Finding 1 (grep -q under pipefail SIGPIPE race):
  audited scripts/ and workflows as it suggests. Result: clean —
  deploy.yml's two `| head -1` pipelines are exit-status-safe (one
  swallowed by echo's command substitution, one has `|| true`), and the
  keystore script's -q flags are pipe-free. Finding 2 (CLAUDE.md holds
  operative rules, not investigation records) — noted; our TOOLCHAIN
  TRAPS section is rules-first but yesterday's additions skirt the
  line. Candidate for the post-beta REQUIREMENTS/docs pass rather than
  churn now.
- Rollup artifact republished (same URL) with the settled/new items.

### Housekeeping shipped (N=30, his answer)
Done cards over 30 days old file themselves to Archive at launch, at
every depth. RED-zone: 3 tests red-first (aging, depth+cold-start
idempotence, and the leave-alones: already-archived, trashed, and
done-cards-with-no-date — never guessed at). Witnessed by a quiet
launch toast, because cards changing rooms unwatched would breach
"state is place"; store exposes `lastHousekeeping` for it. E2E: seeded
a 45-day-old done card, saw the toast and the archivedAt land. 104/104.
Paid-tier threshold idea recorded, not built. Shake verification is
his (item 12 stays open pending his pocket).

### Roadmap planning pass (Xian's standing assignment)
Two docs in the new docs/plans/:
- **First pass over every upcoming step** — refinements plus the status
  corrections the week produced (undo toast superseded upward; Item 16
  quiet mode UNBLOCKED by the new undo; R0.2's depth material seeded by
  the felt trash pile; search/undo/housekeeping shipped). Ends with a
  suggested working order for discussion.
- **R2.1 root canvas deep dive.** The organizing insight: the root
  canvas is "the interior of no card" — v3 just removes the root's
  exceptional shape (`cards: Task[]` → `decks: InteriorDeck[]`), so the
  schema change deletes an exception rather than inventing a concept.
  Four shippable stages (invisible migration → minimal deck switching →
  move-between-decks via promote-at-top gaining a meaning → background
  pan with rooms at the right end, "the afterlife is to the right"
  rhyming globally with swipe-right locally). Five design questions
  reserved for Xian, incl. whether root decks can themselves complete
  (my lean: no — a completable project is already a card). Biggest risk
  named honestly: the migration touches his live deck — fixture corpus
  against his real backup shape before any code.

### Quiet mode shipped (his answer: "I mostly don't like them")
Item 16, wanted after all — the name "quiet mode" was the capture doc's
coinage, not his, which is why it didn't ring a bell for him. Settings
switch mutes success/info/message toasts; **errors always surface** (a
muted failure report is m-44 in a new costume — the gate structurally
cannot touch toast.error, and a test pins that). Gate lives in the one
module every call site already imports from — zero call-site changes.
4 unit tests + full E2E (complete→toast; toggle; complete→silence; data
still mutates). 108/108.

**Correction to yesterday's record, caught by today's diagnostics:** my
"menu Undo verified" E2E had actually clicked the *toast's* Undo button
— the toast was still on screen at click time and getByText('Undo')
matched it first. The hold-on-card menu doesn't contain Undo at all
(it's the per-card menu; Undo lives in the deck menu, hold on
BACKGROUND). Re-verified properly today: toast drained for 5.6s first,
deck menu opened at a background point, Undo clicked there,
un-completion confirmed in data. The lesson is the A/B-control lesson
in miniature: a selector that can match two things isn't evidence about
either.

Stamped **rc.14** (housekeeping + quiet mode are behavior past the
rc.13 stamp — the drift rule applied same-day, unprompted this time).
Beta remains rc.12; no cut memo needed until a cut is asked for.

### Model conversation with Xian — topology settled
The most consequential exchange of the week, and it produced no code:
- **Multi-root-decks = PRO, decided** (ordinary users lose little; a
  power user's feature). Free = one root deck; wall gates count, never
  shape.
- **Decks never complete, at any level** — his formulation is now the
  canonical one: "a deck can be empty because it has no incomplete
  cards, but that does not mean it is done."
- **Decks need no lifecycle at all**: a deck's only state is who holds
  it (parent: none | card-id). Promotion/demotion = change of holder =
  metadata, not lifecycle. A thing deliberately not built.
- **The alternation is now explicit doctrine**: table → deck → card →
  deck → card, never skipping. His "we've made it a little magical"
  worry dissolves into one rule applied twice. Corollary surfaced:
  future root-deck demotion needs a carrier card.
- **Strip exists for free users too** ([deck] │ rooms + rightward
  hint — it's how the R2 discoverability gate is scored); pro adds
  decks left of the divider.
- New standing preference (memory + practice): embed GitHub links when
  citing repo docs in replies — he reads from Claude apps where bare
  paths aren't linkable.

### Shades shipped (his whim, same hour) — rc.15
Cards in the rooms are now *shades* — his word, and the right one: the
rooms are already "the afterlife," so the cards there look the part,
progressively washed out (Done barely — a trophy still warm; Archive
paler; Trash near-gray and receded into the table). Placeholder for the
aging design pass, which now has THREE strands of one material
language: volume=thickness, time=patina, state=vitality.

Craft note worth keeping: first attempt used pure desaturation and was
INVISIBLE — the card face is white with near-black ink, no color to
remove. Caught by screenshot, not assumption (the probe's whole point).
A shade loses vitality: opacity letting the table breathe through +
contrast pulling ink toward gray. Aging (Item 22): fun shelf, R2-era,
nothing blocks it — Xian asked.

### R2.1 Stage 1 SHIPPED — the root is a deck (schema v3, invisible)
Xian green-lit building during beta week, then went heads-down. Fixture
corpus first (6 tests red), then migrate.ts v3 (v2 cards → root deck
"deck-1", byte-identical; v1 chains through; future versions pass
untouched), then the store: `decks: InteriorDeck[]` is the source of
truth and `tasks` became a get/set accessor over deck[0] — every line
of single-deck-era code works unchanged through it, while find/
housekeeping/emptyTrash/undo/snapshot-restore walk ALL decks. `.v2backup`
paranoia copy written before the first v3 write, mirroring v1backup.

118/118 (10 new: fixture corpus + store-boundary tests incl. cold-start
idempotence and undo across the migration). The Item 15 guard refused
my test fixture AGAIN (completing a parent over an open sub-card) —
second time the invariant has caught its own test suite. E2E: a
device-shaped v2 document upgrades on load (v3 + deck-1 + untouched
backup verified in storage), UI pixel-identical behavior — reveal,
defer, re-deal all normal, mutations writing v3.

Stamped rc.16 + tag. Mid-build note from Xian recorded as doctrine:
comp/pro entitlement must be per-DEVICE (his), never a build default —
retires my "severable but enabled" plan; `hasPro()` seam lands with
stage 2's first gated chrome, default free everywhere.

### R2.1 Stage 2 SHIPPED — decks exist (rc.17)
Store: getDecks/createDeck/switchDeck/renameDeck/deleteDeck +
activeDeckId. The `tasks` accessor now routes to the ACTIVE deck (a
device preference stored beside the document, never in it — backups
must not carry one device's viewpoint). deleteDeck refuses non-empty
decks and the last deck (sealed-card lesson generalized: no operation
may bury or discard cards as a side effect); deleting the active deck
walks the pointer home; a stale/undone pointer self-heals to deck[0].
createDeck slugs deck-N, import-N's sibling. 8 tests red-first, 126
green.

Entitlement seam (`hasPro()`): per-DEVICE grant via ?pro=comp / ?pro=off
URL param — the pre-business write path; the read seam is permanent.
The build never special-cases anyone (Xian's crisp-boundary doctrine).
UI: "Decks…" leads the hold-menu ONLY when the device has >1 deck or
pro (no cruft till needed); sheet lists names — no counts, covenant 7
applies to decks too; + New deck is the sole pro-walled act. Rooms
gather from ALL decks (a done card in deck-2 must never vanish from
Done while deck-1 is active — the 07-07 trust bug's new shape,
E2E-proven).

Ten-step E2E proves the whole story including both directions of the
wall: free hides the entry; pro reveals it; create+switch+isolate;
cross-deck Done room; reload persistence; revoke keeps switching but
removes creation (data not hostage).

**Verification lesson for TESTING.md's list: `addInitScript` re-runs on
EVERY navigation.** My probe's "reload" step silently re-seeded storage
and manufactured a vanishing-deck bug that sent me hunting through the
store (which a vitest cold-start repro then exonerated). Reload
semantics need a second page in the same context. The bisect that
"found" the drop at reload was the harness observing itself.

### Item 7 closed + R2.1 Stage 3 SHIPPED (rc.18)
**Item 7** under the standing default: new domain helper
`pathToUnfinished` (4 tests red-first) — returns the full stack of
levels to the nearest open card, direct-child decks preferred over
deep ones so the shortest reveal wins. Both Index branches descend the
whole path now. E2E: the buried-grandchild case lands you LOOKING at
"Write the changelog" instead of being told about it.

**Stage 3**: `moveCardToDeck` (3 tests red-first — top-of-target via
topSortOrder, whole interior rides along, and the refusals: unknown
card/deck, same-deck no-op, nested cards told to promote first).
UI: "Move to another deck…" appears on a top-level card's hold menu
only when another deck exists — promote-at-top gaining its meaning.
Picker reuses the ActionSheet language, lists only OTHER decks.

133/133; seven-step E2E across both features. rc.18 tagged.

### R2.1 Stage 4 v1 — canvas strip IN PREVIEW (rc.19, ?canvas=on)
The most design-visible piece of the sequence, so it ships behind a
device-local preview flag rather than onto Xian's daily driver — the
reversible-alternative umbrella applied to design risk. Scope: the
spatial MODEL — neighbors peek in as thin slivers of real card-back
material (the affordance IS the neighbor), the afterlife as a gray
shade-edge at the far right ("the afterlife is to the right"),
tap-to-pan, slide transitions; ChainView reached by panning past the
last deck, with a peek home. Deliberately deferred pending his verdict:
free background-drag (highest gesture-collision risk) and rooms
placement. Flag OFF is E2E-proven identical to today. Screenshot of
the work-deck page with both peeks is the best single image of R2 so
far: warm card-back sliver left, gray shade sliver right, zero chrome.
133/133; six-step E2E incl. flag-off regression.

### R3.1 SourceAdapter seam — defined and proven (no stamp; no UI yet)
The seam every external service comes through: `SourceAdapter`
(service slug + read-only fetchCards — write access deliberately absent
from the contract until R3.4 earns it), proven by DemoSourceAdapter.
`importFromSource` lands cards in a ROOT DECK named for the service —
R2.1 made the roadmap's promised landing zone ("a source deck on the
canvas") a real place, a convergence I didn't plan but will take.
Provenance is the only upstream identity: local ids always regenerated
(the shadow-import lesson upheld by a test), {source, externalId}
carried for dedupe; re-import is idempotent; late upstream arrivals
join the existing deck. Store gains the one honest write path the seam
needs (addCardsToDeck — mechanism, not policy; my first draft persisted
via a rename-to-same-name hack and was replaced before commit).
4 tests red-first; 137/137. R3.2 now waits only on Xian naming his
service.

### Day ledger (2026-07-29): rc.12 → rc.19
Search+undo+trash decisions (13) · housekeeping+quiet mode (14) ·
shades (15) · v3 root-deck migration (16) · decks + pro wall (17) ·
deck moves + block-reveals-at-depth (18) · canvas strip in preview
(19) · R3.1 seam (unstamped). Beta week untouched on rc.12 throughout.
