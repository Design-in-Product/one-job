# Coral Log: 2026-08-29

## Session Start

### Context
- Environment: Claude Code, Amber (Fable)
- Project: One Job
- Git Status: clean
- Current Phase: instrumentation plan (docs/plans/2026-08-28-
  instrumentation-plan.md) awaiting Xian's review — 4 open questions
  (checkpoint cadence, install-ID comfort, Settings visibility, build
  sign-off + ordering vs fleet probe). R2.3/dark-mode proposal also
  awaiting his confirmation.

### Brief processed (via origin/main monitor, task b8b9ecwem)
`docs/briefs/cross-pollination/current.md`, 2026-08-29: **Klatch Rule
14 — when you narrow a clause, recompute every number the old clause
generated, in the same commit.** A stale number framed as a finding
("we found X") is more dangerous than a broken test because it never
shows up red.

**Applied for real, and it found something**: the rollup's dependency
item still said "4 remain" — computed 08-13, against a dependency tree
the 08-16 lockfile surgery then rewrote (vitest.vite override pin,
full regeneration). Recomputed with `npm audit`: **7 current** (6
moderate, 1 high). The original 4 unchanged (vite/esbuild/
react-router×2, held for major bumps, reasoning intact); 3 new entries
are one chain — `@capacitor/cli → xcode → nested old uuid` — all
devDependency build tooling that runs only on Amber during native
builds, zero shipped exposure. Same hold posture; the number is now
true. Rollup updated with the recount ABOVE the original analysis
(kept unedited for the per-package reasoning), commit d71f86a — the
recompute and the acknowledgment of the narrowed clause in the same
commit, per the rule's own letter.

Also noted: the instrumentation plan already embodies this rule's
defense by design — metric definitions as executable functions rather
than prose findings, explicitly so numbers can't survive definition
changes via framing.

No other action items.

## Session #2 (3:52pm): roadmap reordered, next-steps discussion opened

Xian checked in (dictated, transcription clean): plan approved ("looks
good"), asked whether ROADMAP.md captures the latest thinking — honest
answer was NO, it was stale in both directions: didn't record what
shipped (R0/R1 complete; R2.1 canvas + R2.7 inchworm live since July;
R3.1/R3.2 GitHub import live since rc.21 — all still listed as future
work) and didn't record the 08-26/08-28 pivot at all.

Rewrote it (commit 6eae0fa): new "The pivot (2026-08-28)" section with
the P0–P5 sequence as a table (each with current state), dated status
annotations on every stage header rather than silent rewrites, R1.5 as
a real roadmap item pointing at the instrumentation plan, R4 reframed
as the strategic center with R4.2-lite as the entry, the completed
07-04 ten-move build sequence closed out and replaced with the current
seven moves, Xian's R2 hedge recorded verbatim, R2.3+dark-mode marked
proposed-not-confirmed. The 07-04 baseline history left intact and
annotated, not erased.

Next: present the next-steps/milestones picture he asked for, plus the
not-yet-fully-discussed items from his research dump.

## Session #3: "settled" → build 33 uploaded; probe greenlit → built; instrumentation Q&A owed

Xian's four answers, actioned in order:
1. **"settled"** → native build 33 cut immediately (32 predated the
   shake removal and its reviewer notes): bump → build:native → sync →
   archive → export → upload, all clean on the proven pipeline.
   Delivery UUID 42326745-068d-4106-8744-7d772e28fe73. Commit 62673a6.
2. Instrumentation questions: he wants a conversational walkthrough
   with recommendations + arguments — delivered in the reply, decision
   pending his responses.
3. R2.3/dark-mode: he asked what the question even is — explained in
   the reply (what the two items are, what delegation means here, what
   he's deciding).
4. **Probe greenlit** → `scripts/fleet-probe.mjs` written and run:
   parses the rollup's Open section, emits v2-backup-shaped JSON,
   imports via the EXISTING "Bring in as a new deck" path — zero app
   code, the memo's crudeness ceiling honored. First deck generated:
   10 cards (1 🔴, 5 🟡, 4 🟢), provenance-stamped. Commit 2c6a573.
   Delivery options given (paste-import from chat, or the committed
   file). Two-week window starts when he imports.

Standing guidance recorded: **pricing derives from research, not the
other way around** (P5's conversations are the instrument), and
**iterate the pitch materials as plans evolve / milestones hit** — the
.pptx/.docx in docs/pitch/ are now living documents on my maintenance
list, next touch when build 33 clears review or the probe produces its
first signal.

Remaining before actual submission: fresh screenshot capture+compose
against rc.33 (the 08-04 staging set predates deck identity), upload
to Media Manager, release notes, select build 33, submit — flagged to
Xian as the next concrete milestone gate.
