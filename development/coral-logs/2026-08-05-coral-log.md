# Session Log: 2026-08-05

## Session Start: ~6:53 PM (Xian)

### Context
- Model: **moved to Opus 5** for a few hours (Fable's weekly cap hit).
  Identity is model-independent — it lives in these documents (his
  words, 07-08); noting the change, not adapting to it.
- `main` at a757380, clean, in sync. Overnight: Janus's 08-05 brief
  (which carried MY rc.23 finding network-wide — "an empty result is
  indistinguishable from 'no data found' and harder to diagnose than a
  loud refusal"), and Pard's stand-down runbook review request.
- rc.24 live on web; rc.12 with testers (Dan pending recovery).

### Xian's three topics for this session
1. Testing the TestFlight RC + handoff
2. Potentially retiring Relay
3. **Amber restart** — forces new sessions for all agents, and moves me
   to the correct Claude config on Amber. Pard will have a message.

### Pard's review request (§10 asks me one question directly)
"You raised the Xcode-update-forces-macOS-update coupling on 07-30.
How often do you expect this to fire, and does anything in One Job's
build pipeline need to be QUIESCED before a reboot rather than just
handed off?" — answering with a real cadence estimate and the
quiesce/handoff distinction, which is the sharper half of the question.

### Pard's runbook reviewed; §10 question answered (delivered + verified)
**Cadence**: 2–4 forced reboots/year in bursts, not a drip — Apple's
minimum-SDK rule tightens ~annually (spring) plus autumn OS releases;
well-signposted (a submission error names the version), days of warning.
**Quiesce**: essentially nothing — all my build state is derived and
regenerates in <1 min; source of truth is origin/main. The useful
exception: **a resident may be mid-EXTERNAL-TRANSACTION** (xcodebuild
archive / ASC upload) where interruption costs 10–20 min and can leave a
half-uploaded build needing manual ASC cleanup. Proposed a "busy —
non-resumable external transaction" hold that DELAYS the gate rather
than waiving it; "resumable" and "free to interrupt" are different
properties and only the resident knows which they're in.

**Finding for §10.5, checked not assumed**: my own CLAUDE_CONFIG_DIR is
`~/.claude-kindsys` — the RETIRED account's config (which is why my
persistent memory has been writing there all week). A faithful tmux
snapshot would faithfully restore the mistake. Proposed one procedure
line: confirm each captured config dir is the one the resident SHOULD
have, not merely the one they had — turning the roster from a
photocopier into a checkpoint. Xian's planned config move becomes a
runbook step rather than a remembered favour.

Volunteered for the first rehearsal batch: cheapest restore on the
fleet (no pending external transactions, nothing outside git), handoff
already green on the trunk.

---
## Xian's rc.24 test results — 2 real bugs found, both fixed (rc.25)

**#3 undo "did nothing" — ROOT CAUSE FOUND, and it wasn't the shake.**
Could not reproduce on desktop (undo works fully: data + view + toast +
face-up card). Wrote a store-level test instead and it went RED
immediately: **the launch housekeeping sweep was pushing onto the undo
stack.** So on his phone — where iOS discards PWA pages aggressively —
a reload runs housekeeping, `canUndo()` becomes true with NO user
action on the stack, the shake prompt appears (correctly gated on
canUndo), and Undo then reverses THE SWEEP. The completion he wanted
back was in a previous, discarded session and never returned. Every
symptom explained. Fixed: housekeeping saves with capture suppressed —
undo reverses what the PERSON did, nothing else may occupy that stack.
(Hoisted `capturingUndo` since housekeep runs from the constructor.)
2 tests pin it.

**#4 inchworm stuck in a sub-deck — fixed.** The walk already contains
every interior card, so descending from inside it strands you in a view
the walk doesn't own. In inchworm mode the sub-deck badge is not
offered and a tap reads the card instead of changing altitude.
E2E: leaf on top, no badge, tap→details, escape→back on the walk.

**#1 comp code again** — expected, not a bug: separate storage
containers (Chrome vs home-screen PWA) each need their own grant. Worth
noting the friction; the in-app code exists precisely because URLs
route unpredictably.

**#5 rooms search** — answered: appears only at 6+ cards in that room
(deliberate: small piles stay pure sift). His question is evidence the
threshold is invisible; candidate to lower or hint.

**#2 deck identity** — real gap, filed: with 2+ decks nothing tells you
which deck you're in. His suggestion (names on the backs, distinct
colors/patterns) is the right shape and touches CardBack + the strip.
Not shipped tonight — it's design work, and the Amber restart is
imminent.
