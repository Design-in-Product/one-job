# Session Log: 2026-07-31

## Session Start: ~1:00 PM (Xian)

### Context
- `main` at 0bd388a — overnight: Pard's five Xcode answers, Relay's
  rc.12 status memo + logs, 07-31 brief (generated-artifact-vs-source;
  corpus-as-predicate-authority).
- **Laptop rebooted; Relay is un-wedged and cutting.** Key decision in
  Relay's log — Xian's tiebreak, correctly exercised: **rc.12 ships
  first** (Dan Brodnitz's feedback must be attributable to the MVP set;
  rc.22's surface would confound it). My rc.22 memo stands for the
  follow-on cut, not this one. Relay knows to checkout the rc.12 TAG —
  they named the build-from-HEAD trap themselves.
- Relay field-verified two of our fixes on a fresh Mac: the
  package.json allowScripts approvals held (no esbuild trap), and the
  double-source version check did real work (three memos in the chain;
  pull-first caught it).
- Relay's near-miss finding worth the brief: renaming a stale clone
  does NOT retract it from GUI recent-items lists — the hazard outlives
  the remedy. Their rule: open via `npx cap open ios` from the
  known-good dir, never a recent item.

### Xian's five answers (with Pard's, they close the consultation)
1. **Xcode = Amber host capability** — OptiListen is a real second
   user today (stronger than Pard's "Klatch someday"). Pard records it
   in the harbor manifest; install/host = Pard, pipeline = me.
2. No bare-CLT dependents (Xian + Pard's resident audit agree);
   rollback one-liner recorded in the manifest.
3. Secrets: Pard ADOPTED a harbor convention same-day (tool-canonical
   path, 700/600, never in repo, manifest secrets registry, xian-only).
   The .p8 will be entry #2.
4. **Amber is the pipeline's natural home** (Xian) + consolidate
   (Pard's infra read, with the honest cost named: App Store minimums
   force Xcode→macOS updates = fleet reboot events, 14 residents).
   Relay = cowork agent for operating the computer; functionality
   migration open. Rollup item 5 has its DIRECTION.
5. Maintenance: Pard's, daemon-class, courtesy windows; I flag
   version-minimum submission errors the moment one appears.

### Session Objectives
1. Close consultation loop: runbook conforms to manifest division;
   rollup item 5 updated; briefing artifact refreshed (rc.12-first).
2. Stand by for Relay's cut; respond to smoke findings if any.

---
## Session #2: Aug 1, ~6 PM (Xian: Relay status? + flag reminder)

### Janus's delivery-verification memo → immediate repair + memory
My 7/28+7/29 DinP mail copies sat UNTRACKED in the hub checkout for
three days — receivers' drains read origin/main; a file on disk is not
delivery. Checked mediajunkie: same bug on both 07-30 Xcode memos
(Pard answered anyway — working-tree luck, not delivery). Repaired per
the rule: exact-filename adds (another agent's untracked memo sat
beside mine — a sweep would have committed their mail), visitor
identity, push, and THE step — verified both via
`git show origin/main:...`. Rule saved to persistent memory.

### Relay status (Xian's question): stalled at session start
Relay's 07-31 log ends at 12:57 PM Friday with session start — no
activity entries, no upload memo, nothing on main since. Their queue
was entirely Xcode-GUI steps (signing check → build bump → device
smoke → archive → upload) with the computer-use MCP down, meaning
Xian's hands were the actuator. Conclusion: stalled awaiting Xian,
almost certainly right at the first click. Resume point per their own
07-29 memo: `npx cap open ios` — nothing lost, no rebuild needed.

### Trello decided next (Xian, 08-01), Gall-gated on his GitHub test
His taxonomy insight recorded in the roadmap plan: project management
is not task management — nobody runs their personal life in GitHub
issues; his ACTIVE Trello boards are the "I'd use them more through a
One Job front end" story, which is the R3 gate's own phrasing arriving
unprompted. Build gate: his real-use GitHub test first. Queued design
question: Trello's board→list→card vs our deck→card→deck alternation —
lists are the wrinkle (carrier cards? named interior decks?). Briefing
artifact card flipped to settled.

---
## Session #3 (Aug 2): rc.12 ON TESTFLIGHT
Relay delivered: build 1.0 (2), co.onejob.deck, uploaded 08-01 18:42
PDT, confirmed by observation (Organizer "Uploaded to Apple"). First
build past rc.2 since 07-03 — the dark stretch is over. The rc.13-memo
discipline paid off in their hands: the rc.12-first answer was already
filed; the anticipated memo conflict didn't exist.

**Their two findings, actioned:**
- **The BUNDLEID trap RE-ARMS PER CHECKOUT** — Team lives in
  project.pbxproj; any ref switch resets it to None, and tag-based
  cutting makes that every cut. Brief updated in the trap box itself.
  One keystroke separates the paid team from the permanent-damage one.
- **Stale issue navigator as false evidence** — Reset Package Caches
  worked while the error list still showed the previous build's
  results; only a re-run refreshed it. "An indicator that hasn't
  refreshed is not an observation" — same family as our export-toast/
  m-44 findings. Flagged for Janus via log.

**Sequencing inversion (Xian-approved, and right):** upload first,
Xian smoke-tests the ACTUAL TestFlight artifact (dev-signed overwrite
risks delete-and-reinstall — a data-loss path), then Dan. Briefing
artifact updated with his 5-step smoke checklist.

**Continuity alarm:** faoilean → kindbook move means Relay's session
dies with no memory store (they checked — none exists). Their handoff
memo (Janus's 07-21 ask) is now the critical path; nudged by memo with
the cohort format as template, delivery VERIFIED on origin/main.
Android thread noted: signed-AAB workflow offer stands, untaken.

### (Aug 2 AM) TestFlight build invisible → Missing Compliance diagnosed
Xian: new build not showing on his phone, 36h after upload. Root cause
visible from the repo: ITSAppUsesNonExemptEncryption unset in
Info.plist → every build parks at "Missing Compliance" in ASC,
invisible to testers until the encryption question is answered by
hand. Walked him through the 60-second ASC fix for build 1.0 (2);
committed the permanent fix (key = false — One Job uses only
HTTPS/exempt encryption) so rc.22+ never asks. Also added to the
Relay brief consideration: this was exactly the class of thing their
handoff memo / the brief should carry.

### (Aug 2) Source-dimension survey written (his Trello-planning ask)
docs/plans/2026-08-02-source-dimension-survey.md — eight surfaces
(Trello/Todoist/Asana/GitHub/Reminders/Things/Linear/Jira), seven
dimensions (containment rigidity, status binary/staged/graph,
cross-cutting, time, multi-home, people, checklist-lite children), and
his PM≠task insight formalized: **import is always a projection** —
flattening a workflow into our lifecycle loses data BY DESIGN, and
provenance is what makes the loss honest. On his flexibility musing,
the lean he half-predicted: one blessed mapping per source + at most
ONE import-time question where genuinely ambiguous (Trello's: "are
your lists stages or categories?"); mapping editors are the fussiness
temptation at the worst possible moment. Custom mappings → premium
shelf, maybe-never.

### (Aug 2) Relay-migration requirement recorded (his process pain)
kindbook Relay must delegate GitHub work to code agents — Xian-as-git-
hands is over (the 07-25 undelivered mail was the proof case). Rollup
5a; goes into my migration plan draft (after his smoke test) and the
successor's brief.

---
## Session #4 (Aug 3): DEVICE PASS GREEN — Dan unblocked
Relay's memos: all 8 checks green on the real TestFlight artifact
(Settings-verified rc.12), including both named checks reported as
observed. RELAY-HANDOFF.md filed (318 lines, cohort format) and
verified on origin/main; brief now opens with a pointer to it.

**Their false alarm is the week's best lesson:** two "regressions"
filed from the device turned out to be correct rc.2 behavior — the
TestFlight update had never been installed. Relay checked the CODE
first (byte-identical between rc.12 and main) which ruled out
"behind on fixes" — but skipped the 30-second built-current check
twice. "Establish what is RUNNING before explaining why it behaves
oddly." → tester instructions for Dan now open with the version check
(rollup 19). Same family as stale-issue-navigator; flagged for Janus.

**Handoff's three flags actioned:** keystore = rollup 18 🔴 (only
copies live on faoilean, one in a folder slated for deletion; GH
secrets are derivatives, not backup; observation-verified copy before
the laptop repurpose); Dan version-line = 19; ASC API key as
highest-leverage unblock = 20 (with its honest corollary: it shrinks
Relay to "a physical device in a hand" — Xian's migration input).
Ack memo to Relay delivered and verified.

### (Aug 3, later) Relay ships relay-push.sh
The delivery-verification rule as a one-paste command: exact-scope
staging (store/ + docs/mail/ only — source deliberately excluded),
per-commit visitor identity, and the closing verification (merge-base
--is-ancestor against a fresh fetch) that states the indicator/outcome
distinction out loud in its own comments. Mitigates the Xian-as-git-
hands pain for the CURRENT Relay (one paste instead of four commands);
the kindbook successor with code agents (rollup 5a) remains the real
fix. A session that learns a lesson and leaves it as executable
discipline for its successor — the handoff culture working exactly as
intended.

---
## Session #5 (Aug 4): store-asset pipeline built (Relay's ask)
rc.12 is WITH TESTERS (Dan invited, recuperating — his timeline).
Relay's memo requested fresh store assets; the rc.2-era set depicts an
app that no longer exists. Built per their prep/shutter split:
- `scripts/capture-store-shots.mjs`: 3 device profiles at store
  dimensions, 6-shot list, seeded photogenic deck, one-command
  re-capture — the harness makes assets un-ageable (regeneration free,
  shutter gated on the release-version decision).
- Staging set captured + committed (iphone-67 + ipad-13, rc.22-era).
  Harness debugging: badge is aria-label not text (getByRole);
  SubstackView's back is icon-only. Shot 01 is genuinely store-worthy
  — premise in one image, afterlife peek showing.
- **The iPad deliberate look, delivered**: functionally clean at 13",
  presentation = phone column in a wide field, peek reads as a thin
  bar. Three options filed (honest column / geometry scale / R2 canvas
  as the real answer). Confirms Xian's observation; frames the choice.
- LISTING.md reviewed: two aged spots filed as PROPOSED deltas
  (substack vocabulary; version-dependent review-notes line) — not
  applied, it's his approved copy.
Rollup 21 added (three small calls); memo to Relay delivered.

### (Aug 4, later) Marketing-canvas compositor built (Xian's scoping correction)
His correction via Relay's amended memo: store slots are CANVASES —
device frame + background + caption — not captures; bare captures
would read as a 2019 listing even re-shot perfectly. Relay's
decomposition (captures / composition / caption copy) implemented:
`scripts/compose-store-shots.mjs` takes the raw captures and renders
finished slot images — brand-gradient ground, white headline + sub,
CSS device frame, capture inset — at exact store dimensions. All
version-INDEPENDENT except the inner screen: the shutter is genuinely
the last step and a small one, exactly as the amendment hoped. Six
DRAFT captions (install sheet = first three = the whole pitch) await
Xian's wordsmithing; Figma noted as the alternative canvas if he wants
design iteration there (connector present, unauthenticated).
Full run: capture → compose = two commands, both re-runnable free.

### (Aug 4, evening) Caption slates merged — Relay's headlines win
Relay drafted captions in parallel (benign collision — both of us saw
the same gap). Theirs are better where it counts: "This is your whole
to-do list." provokes the question the screenshot answers, and their
install-sheet arc (premise → gesture → first objection, then reassure)
is real craft. Merged: their headlines + my sublines, ONE rendered
slate for Xian instead of two competing sets; alternates preserved in
LISTING. Re-composed both device sets. Credit where due — the Cowork
agent out-wrote me on the marketing line.
