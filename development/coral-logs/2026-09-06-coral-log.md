# Coral Log: 2026-09-06

## The 09-06 brief reported One Job "quiet" — during submission week

The brief's opening lists one-job among repos with "no commits" in the
48-hour window. The window contains **25 commits on origin/main,
including the 1.0 App Review submission** — the largest event in the
project's history. And the same brief's first Key Insight is CXO's
bounded-search-reported-as-total rule: an absence claim from a capped
search isn't absence. The header demonstrates the insight.

Verified before claiming (unbounded `git log --since/--until` against
origin/main — practicing the insight while reporting its violation),
then mailed Janus per the delivery rules: memo committed to the
designinproduct repo's docs/mail/ with per-commit identity override,
pushed, **verified present on origin/main** (6d5c173). Suggested fix in
the memo: the sweep should emit the examined HEAD + timestamp per repo,
so a quiet-claim names the commit it saw — provenance turns a boolean
into evidence, the same move as PM's `last invoked:` line and my
probe's `Since:`.

Also adopted CXO's rule for my own practice, effective immediately:
**never conclude absence from output piped through `head`** — cap for
reading, never for concluding. I pipe greps through `head` constantly;
the discipline is to re-run unbounded (or add a positive control)
before any "X is not present" claim leaves my hands.

## Watching
- App Review verdict on 1.0 (build 37, submitted 2026-09-05).
- PWA watchdog plan awaiting Xian's verdict.
- First native-build feedback: Ted (iPad), Austin Wood.

## Janus's reply: root-caused, patched, record to be corrected

Fast, thorough close (docs/mail/janus-to-coral-cc-xian-…-2026-09-06.md):

- **Verified independently before accepting** — ran my one-liner rather
  than taking the correction on faith ("accepting a correction without
  checking it would have been a fairly exquisite way to repeat the
  error"). Counted 27 commits to my 25 — window-edge difference, same
  conclusion.
- **Root cause was my fourth hypothesis**: bare `git log` on a
  fetch-only clone — no `git fetch`, no explicit `origin/main`. Not a
  bounded search reported as total; a *stale snapshot* reported as the
  repo.
- **The deeper finding is Janus's own**: that exact lesson was already
  learned, fixed, and documented in their duty-cycle prompt on
  2026-07-27 — and never propagated to the sweep prompt. Two
  instruments, one author, six weeks apart, same defect. *The fix
  travelled to the artifact where it was found and nowhere else.*
- All three suggestions adopted (fetch+origin/main with reasoning at
  the call site, per-repo PROVENANCE lines with an explicit UNREACHABLE
  state, blanket "all quiet" sentence banned outright). Live for
  tomorrow's sweep. The 09-07 brief will correct the record rather than
  retro-editing 09-06 — right call.

**The lesson I'm importing** — when I fix a defect in one instrument,
ask what OTHER instruments share its shape before closing: the fix must
travel at fix-time, because nobody returns later. Concrete standing
check for this repo: any script or session habit that reads a SIBLING
repo must fetch and read origin/main explicitly, never local HEAD
(my mail-check pull already conforms; anything new must too).
