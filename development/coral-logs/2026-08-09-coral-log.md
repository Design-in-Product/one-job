# Coral Log: 2026-08-09

## Brief processed (background; no Xian action needed)
1. **Scope-is-not-direction** (PM merge-drop, work destroyed by an
   endorsed-as-safe `git checkout <ref> -- <path>`): ADOPTED into
   CLAUDE.md's destructive-action protocol — pre-flight
   `git diff HEAD -- <path>` before any content-restoring git command;
   never infer staleness from upstream's existence.
2. **Freeze detector must live outside the frozen set** (PM's Aug 6
   cohort freeze; blackout signal beats staleness signal): noted, not
   adopted — One Job runs no duty-cycle cohort; my Monitor-tool watches
   die with my session by the same principle (known limitation; the
   host-level watchdog layer on Amber is Pard/PM's domain and now
   exists as freeze-watchdog-amber.sh).

## Standing state
rc.31 live; ASC key live (#20 closed); Relay role paused with honors,
capability map complete (one someday-test: gh-CLI-with-token from a
repo-connected session); waiting on Xian: rc.31 device pass results,
Pro-feature feedback (Janus flagged), 1.0 build call, reboot/config
scheduling with Pard.

### Capability map CLOSED — verbatim errors from both shells (Relay, live-tested)
Device VM: generic `403 from proxy after CONNECT` for all egress (deny-
by-default proxy, no allowlist — Relay's precision correction to my "no
network by design" accepted). Cloud container: anonymous read works
(re-confirmed), authenticated write blocked by the named git proxy
(session repo-set). 2x2 map now evidence-backed in every cell; recorded
in the ack memo. Slack bridge remains the one untested-optional cell.
Report arrived via Xian's paste; filed + acked in docs/mail/.
