# Coral Log: 2026-08-12

## Session Start

### Context
- Environment: Claude Code, Amber (post-reboot, default partition)
- Project: One Job
- Git Status: clean
- Recent Commits: cross-pollination brief landed (d65bb43); 08-11 close-out
  (post-reboot checklist, Pard's CronList Q&A, schedule re-arm)
- Current Phase: holding — awaiting Xian's rc.31 device pass, Pro-feature
  feedback, 1.0 build call

### Brief processed (via origin/main monitor, task b8b9ecwem)
`docs/briefs/cross-pollination/current.md`, 2026-08-12:

1. **Klatch/Iris — incomplete AI turns need an explicit status.** Non-clean
   stop reasons (`max_tokens`, `refusal`, `pause_turn`,
   `context_window_exceeded`) were all stored as `'complete'`, making a
   truncated turn indistinguishable from a finished one in the DB. Fixed
   with a typed `'incomplete'` status + fixed stopReason enum (not an
   arbitrary string — avoids the growing-switch-statement miss), reusing
   existing amber/error-slot chrome rather than adding new. **Relevance to
   One Job:** low-direct (we don't store AI turn history), but the
   general pattern — a fixed enum over a free string for anything a model
   or backend might extend — is a reusable instinct.
2. **Mediajunkie/Pard — verification tools that fail open are one defect
   in nine scripts.** Nine reboot-runbook defects shared one shape:
   absence-of-reported-failure read as success (a `verify` returning 24
   errors as pass; a `tar` backup failing open while `rm -rf` ran anyway;
   an assertion checking "launched" not "working"). Fix: allowlist
   success conditions, default to fail/undetermined, not denylist known
   failure cases. **Directly applicable HERE**: this is the same species
   as the deploy-dispatch-guard bug from 08-06 (`workflow_dispatch` runs
   going green while skipping deploy/verify entirely) — caught then by
   checking the live bundle hash instead of trusting run status. No new
   action needed on our pipeline (already fixed, already probes by
   observation), but worth holding as the general lens for any future
   gate script: does its default path require a positive confirmation,
   or does silence pass?

No action items generated for One Job specifically — both are noted,
one as a reusable instinct, one as confirmation our existing fix already
matches the recommended shape.

### Session Activities
- Monitor event (task b8b9ecwem) caught the brief's commit within the
  90s poll window — first live confirmation the re-armed watch works
  post-reboot.
