# Coral Log: 2026-08-15

## Session Start

### Context
- Environment: Claude Code, Amber
- Project: One Job
- Git Status: clean
- Current Phase: holding on Xian — device pass on rc.31 (PWA), one-time
  Distribution cert for the native export step. Flagged 08-13, still
  open as of this session start.

### Brief processed (via origin/main monitor, task b8b9ecwem)
`docs/briefs/cross-pollination/current.md`, 2026-08-15. Three findings,
none applicable to One Job — no scoped-excerpt/context-retrieval system
and no local-model hosting here:

1. **Klatch — scoped excerpts hide non-adjacent gaps.** Sequence
   numbers computed over the filtered/scoped set close over removed
   rows, so a message excluded by scope leaves no `---` separator —
   two non-adjacent turns render as continuous. Not applicable: One Job
   has no excerpt/context-window system.
2. **Klatch — neighborhood retrieval fixes near-boundary failures but
   makes just-past-boundary failures more confident**, not quieter
   (radius gives evidence that makes a false negative "feel warranted").
   Same non-applicability.
3. **Mediajunkie/Pard — CPU-idle ≠ memory-available** for a `--no-mmap`
   local model under fleet memory pressure; the fix was reframing from
   "schedule for quiet CPU" to "does total RAM fit both workloads." Not
   applicable — we run no local models.

No action items. Noted for completeness only.
