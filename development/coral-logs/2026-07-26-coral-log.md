# Session Log: 2026-07-26

## Session Start (Opus 4.8 window; Coral continues)

### Context
- Project: One Job, branch `claude/one-job-project-assessment-jo96x2`
- Continuing the MVP push; Xian dogfooding the web PWA and reporting
  move-into / completion bugs from his real deck.

### The "Layers of Meta vanished" incident — root cause found

Xian sent his **real phone backup**. Traced the report ("I moved Layers
of Meta into a deck but its parent shows only 3 items and I can't find
it; I also can't reach crumlish.me"):

- Layers of Meta lives at DiP → online presence → **crumlish .me** →
  [deck created 2026-07-26T02:45] → Layers of Meta. That fresh deck is
  the signature of `moveCardInto` creating a deck on a target with none.
- **crumlish .me is `completed: true`.** So it moved active work *into a
  completed card*.
- The whole rest of the UI hides completed cards: `SubstackView` filters
  `!completed`, the sub-deck badge counts only unfinished. So crumlish.me
  (and everything inside it — Layers of Meta + its make-videos/karaoke
  subtree) fell off every navigable surface. online presence's badge = 3
  (the three *unfinished* siblings), exactly what Xian saw.
- The **MoveIntoPicker walked the whole tree with NO completion filter**,
  so it offered a done card as a target. That was the hole.

### The invariant, now enforced: a completed card is sealed

Xian's directive: *"we can't allow cards to be added to finished cards or
decks, clearly."* Enforced end-to-end (test-first, RED ZONE):

1. **MoveIntoPicker** now uses new domain helper `reachableMoveTargets` —
   only ACTIVE cards, descending only through active cards (you can only
   move a card somewhere you could *navigate* to). Skips completed cards
   and their whole subtree.
2. **`moveCardInto`** rejects a completed target (defense in depth).
3. **`addSubstackTask`** rejects when the owning card is completed;
   **`createSubstack`** rejects adding a deck to a completed card
   (new helper `findCardOwningDeck`).
4. **Completion guard deepened**: the R1.1b "can't complete a card with
   unfinished interior" block only checked DIRECT children — a card whose
   direct kids were all done but had unfinished *grandchildren* completed
   anyway, burying them. Now uses `unfinishedDescendants` (whole subtree,
   including work stranded under an already-done intermediate). Also fixed
   a latent crash: `blockingDeck` used a `!` assertion that would throw
   when the blocker was deeper than direct children; now optional.

86 tests green (was 78), `tsc -p tsconfig.app.json` clean.

### Still Xian's call (did NOT build unilaterally — RED ZONE / product)

- **Un-burying his EXISTING stranded work.** The guards prevent *new*
  burials but don't heal crumlish.me's current illegal state (done, with
  an active descendant). Recovery path that works today: open Completed →
  the Done room DOES contain crumlish.me (ChainView flattens all depths),
  but it's sorted by completedAt (2026-07-14) so it's near the bottom of a
  ~30-card one-at-a-time pile → **swipe down to sift to it, then swipe
  left ("Un-done")** → it returns to online presence's deck → navigate in
  → Layers of Meta is reachable again (promote it out if wanted).
- **Findability of old Done cards** — the one-at-a-time sift pile is rough
  at 30+ cards. Wants a search/list view (follow-up).
- **Deeper option to consider**: should a completed card that still has
  unfinished descendants be treated as *not done* (return to the deck) so
  stranded work self-heals? Elegant, but it's invasive — badge and
  SubstackView key off `.completed` directly, not `cardRoom`, so it'd
  touch several components. Proposed to Xian; his design call.

### Known cosmetic follow-up
- CardDeck `handleSwipeComplete` animates the card away *before* knowing
  the completion was blocked (onComplete is fire-and-forget), then
  re-deals it. Looks briefly like it completed. Refinement: have onComplete
  report blocked-vs-done so the swipe can reverse instead of fly-out.

### Innovations worth cross-pollinating
- **"You can only move a thing where you could navigate to it"** as a
  single invariant that collapses three separate bugs (count mismatch,
  vanished card, unreachable target) into one rule — and the same rule
  ("a done container is sealed") governs move, add-card, add-deck, and
  complete. One sentence, four call sites.

---
## Session #2 (same day): import-N container, rc.10 cut

Xian's morning verdict: rescued his stranded cards himself; sealing
invariant means (A) self-heal is unnecessary — retired. (B) sift/search
confirmed as the direction, "will help everywhere as decks get bigger."
Wants multi-step undo considered. Declared the build TestFlight-ready.

### Import fix (his call: "import-N" container)
Diagnosed why his whole-deck import "didn't duplicate": old
importAsSubdeck (a) kept original card ids — every imported card was an
inert shadow of an existing card (findCardById returns first match), and
(b) attached as a SECOND deck on the top card — unreachable, since the
badge only opens the first non-empty deck. Invisible by construction.

New behavior (test-first): import lands as a NEW top-level card named
import-1/import-2/... (optional explicit name param on the store method;
renameable in-app like any card), with EVERY id regenerated at every
depth (`regenerateIds`). 88 tests green. i18n copy updated. Stamped
**1.0.0-rc.10**; Relay cut memo written
(docs/mail/memo-coral-to-relay-2026-07-26-rc10-testflight-cut.md).

Roadmap: added R3.6 — import as merge with OS-style per-conflict
keep-both/replace/skip, superseding import-N once provenance exists.

### ⚠ Open hazard: Xian's device may hold a shadow import
If his import experiment ran on the OLD code, his live deck has an
invisible second deck on "Design in Product" full of duplicate-id copies
of his whole tree. Because findCardById walks DiP's decks before later
top-level cards, lookups for e.g. "health"/"novel" cards could hit the
SHADOW copy first — completions/deferrals silently landing on the copy.
His pasted 06:25 backup is CLEAN (single deck on DiP), so it predates
the import. Asked him to export + share current state so I can check for
duplicate ids; if present, options are a one-off healer written against
his actual data (no heuristics — I drafted and REJECTED a generic
auto-healer after finding it could misflag real cards as shadows) or a
staged restore from a verified-clean backup.

### Next after the cut (agreed direction)
1. Searchable/browsable rooms + general sift (his B).
2. Multi-step undo stack.
3. Then R2.1 root canvas.

---
## WHERE WE LEFT OFF (closing note, written 2026-07-28 — session was interrupted here)

rc.10 was stamped, committed, ff-merged to main, and pushed; the Relay
cut memo was in docs/mail/. The session ended right after that push.
Open threads at interruption, none started since:

1. **Shadow-import check (⚠ hazard, blocks trusting his live deck):**
   waiting on Xian to export + share his CURRENT backup so I can check
   for the duplicate-id shadow deck on "Design in Product" (his 07-26
   06:25 backup was clean; the old-code import ran after it). If dirty:
   one-off healer written against his actual data — the generic
   auto-healer was drafted and REJECTED (could misflag real cards).
2. **Relay TestFlight cut from rc.10** — memo ready, not yet triggered.
3. **Beta week = R1 gate** ("no card lost, stranded, unrecoverable").
4. **Next builds agreed:** searchable/browsable rooms + sift → multi-step
   undo → R2.1 root canvas.

Xian's 07-28 update: he'll work steps 1–4 himself before any cutover;
still manually testing rc.10 ("probably ready"). New context: Coral
migrates to Amber (Mac Studio) via a kindsys.us bridge account expiring
Thu 7/30 — see Pard's memo of 07-28 and today's log.
