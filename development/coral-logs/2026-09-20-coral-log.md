# Coral Log: 2026-09-20

## Session: continuation of the fresh post-renewal session (xian-paced)

Woken by the mail/commit monitor, not by schedule: Janus delivered the
2026-09-20 cross-pollination brief (064d9c0). Pulled, read, audited.

## Brief audit — no exposure, pattern already on the right side

**Insight 1 (Klatch: env-var levers retire source-patching test
probes):** audited whether any One Job test or script rewrites shipped
source to create a measurement seam. None does — grep for write
patterns across `src/**/*.test.*` returns nothing; the only scripts
with `writeFileSync` are `fleet-probe.mjs` (deck output) and
`sync-demo-assets.mjs` (build artifact sync), neither a test probe.
Our three source-reading tests (bootWatchdog → index.html,
metricsStore → source, the shortcutsInbox bi-source pin → Swift + TS)
are read-only assertions — the compensating pattern, not the
anti-pattern. Klatch's design discipline (read per call, throw on
invalid, explicit arg wins) is noted for the day we DO need a test
seam; the answer is a lever at the boundary, not a patch.

**Insight 2** is our own 09-19 finding reflected back (audit
instrument carried the audited defect; cross-language slot pinned) —
nothing new to apply.

## Monitors — the inheritance question resolved by evidence

Yesterday's arrival block recorded "monitors did not survive the
clear" based on an empty TaskList. That was wrong in one case: a
background monitor watching **origin/main for non-Coral commits +
store/docs-mail touches** fired this morning on Janus's brief commit.
Monitors evidently don't appear in TaskList/TaskGet — absence of
listing was not absence. (Insight-3 practice, self-applied: my absence
claim named the wrong surface searched.)

Re-justified, kept: it is the mail/brief arrival bell, exactly right
for a xian-paced seat with no scheduled fires. The second monitor the
predecessor ran remains unobservable — if it fires I'll evaluate it
then; if it never fires it is inert and costs nothing.

## Standing state

Build 40 still held pending Xian's Siri samples; Teresa on 39; CI
green; nothing new owed to anyone. No mail beyond the brief.
