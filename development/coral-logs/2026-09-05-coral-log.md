# Coral Log: 2026-09-05

*(Session continuous from 09-04; date rolled over mid-work. Xian is
working through the ASC submission.)*

## Screenshot review turned into two real app fixes

Xian asked for an artifact showing all 18 shots so he could eyeball the
set before uploading. Built it (18 downscaled JPEGs inlined as data
URIs, ~0.6MB, grouped by profile with captions beside each image and
the install-sheet three tagged). **Reviewing them found two genuine app
defects that no test would have caught**, which is an argument for the
review artifact itself.

### 1 · Usage metrics leaking into a store screenshot (rc.36)
Chasing an unexplained byte-diff on `ipad-13/06` — a file nothing I had
changed should have touched — found the Settings shot rendering R1.5's
"Your usage" panel with counts generated by the capture automation:
*Cards created: 0 · Cards deferred: 1 · Deepest deferral: 1 passes on
one card.* Non-deterministic, meaningless to a stranger, and sitting
directly beneath a caption reading "No account, no cloud, no tracking."

Visible only on iPad — the taller viewport reaches the panel — so a
phone-only check could never have found it. Fixed by clearing
`oneJobMetrics` immediately before shot 06, so the store shows what a
genuinely new user sees.

**Prediction I got wrong, recorded because it mattered:** I told Xian
the panel was "cut off below the fold" on iPhone. The monitor then
showed both iPhone profiles changing too, contradicting me. It was
never below the fold — it was *displacing the Backup section out of
frame*. With it gone, the iPhone Settings shot now shows all four
backup controls, so the image finally delivers its own caption
("Backup is a file you own"). Also retracted my snap judgement, given
minutes earlier, that shot 06 was "the weakest of the six" — it was
weak *because of the bug*.

### 2 · Sub-deck view collapsed instead of filling the screen (rc.37)
Xian, reviewing the set: *"some are top aligned with the (+) floating
high and some are bottom aligned… maybe we need center-alignment in the
app?"* He offered it as a design choice. It was a bug, and the app
already wanted what he asked for.

The sub-deck branch wraps `SubstackView` in a `motion.div` (slide-in
animation) carrying **no flex classes**. As a flex child it defaults to
sizing to its content, so `SubstackView`'s own `flex-1` and
`TaskStack`'s `items-center justify-center` had nothing to fill or
centre against. The whole view collapsed to content height and sat at
the top. The main deck has no such wrapper and was centring correctly
all along.

One line (`flex flex-col flex-1 min-h-0`). Every view now centres its
card with "+" pinned at the bottom — covenant 1 says a card is a card
at every depth, and the layout finally says so too. This also **retires
the "sub-deck top-aligns" note I had filed as a post-1.0 design
question**: it was never a decision anyone needed to make.

Set re-shot against rc.37 → `staging-2026-09-05`; 09-04 retired so only
one candidate folder exists; walkthrough and rollup paths updated.

## Design discussion: R2.2b, wide-viewport peeks
His proposal from the iPad shots: on viewports that float in horizontal
space, continue the peek out to a full card outline with a gradient
fade "so nothing further is expected past it."

Grounded it in the code before responding: `CanvasPeek` pins a 14px
sliver to the CONTAINER edge (`max-w-md mx-auto`). On a phone that edge
IS the screen edge, so the sliver works by implication — clipped,
therefore continuing. On iPad nothing clips it and the implication
collapses.

Argued his version over the cheaper alternative (pin to the viewport
edge instead), because on a wide canvas the truthful statement is
"there is another deck, over there," which a card shape can say and a
distant sliver cannot. Added two constraints: outline-and-hue only,
never content (content would make it the postponed R2 canvas view
arriving disguised as polish); and the fade must *terminate* rather
than soften, since a hard edge implies another card past it. Raised one
question I can't answer: whether a full outline still reads as *the
afterlife* on the grey variant without a label. Recorded as R2.2b with
all of it.

## Considered reply to Ted, drafted
He asked for thorough rather than quick. Ordered by how much each item
moved something rather than by Ted's own list order, which puts
`card-types: task, rule` first — the most valuable line in the email,
and Ted almost certainly doesn't know it.

Substance rather than acknowledgement on the hard ones: `defer [why]`
gets the real constraint (deferring must cost nothing or the kindest
gesture becomes an interrogation) plus an honest "unsolved, ideas
welcome"; the A/B ambiguity is named openly including that Xian and I
read it differently. Gesture grammar quoted verbatim from VISION.md so
nothing overstates what exists.

Deliberately withheld: dated promises; any claim that the
responsibility framing is adopted (it is a *candidate* on the roadmap,
his call to make, not mine to announce); and the submission timing,
since I don't know when he wants that public. All flagged in a
delete-before-sending note, along with two things for him to verify.

## Next on my board while he submits
- PWA service-worker fix design — the long pole gating the 50-user
  cohort, and I owe him a plan rather than an improvisation.
- R2.3 + dark-mode delegation specs (his approval condition — R1.5
  building cleanly — was met on 08-29).

## Brief processing — both insights were live here

Today's cross-pollination brief carried two findings, and unusually
both landed on code in this repo rather than on theory.

### 1 · The hoisted-lookup trap — already fixed, but *verified* today
Klatch's Daedalus found that hoisting a lookup out of a loop silently
breaks dedup when the loop creates the things being looked up. That is
**exactly** the rc.35 `sourceAdapter` bug: `known` was built from
`deck.cards` before the loop and never grew, so two identical entries in
one batch both imported.

The fix and a comment forbidding re-hoisting were already in. What I had
not done is confirm the test could actually *fail* — and the brief's own
point is that the cleanup-shaped change reverts protection silently, so
a passing test proves nothing on its own. Mutation-tested it: deleted
`known.add(e.externalId)`, both dedupe tests went red naming the exact
assertion; restored, six green. **The guard is real.** Ten minutes to
convert a belief into a fact.

### 2 · "Silent" vs. "silent since when" — a real gap, now closed
PM's cohort found that a monitoring alert reporting silence can't
distinguish *never started* from *practice that died*; the fix is
printing when the thing last ran. Exec's framing: *"the difference
between an instrument and a fire drill."*

The attention rollup had the identical defect and I hadn't seen it. A
🟡 marker says an item is open **now**. It cannot say whether the ask
went up yesterday or five weeks ago — and both deal onto an identical
card. **That is precisely the staleness Xian reported** after living in
the first deck ("some items seemed possibly stale"). He was describing
a missing timestamp; I read it as a missing *audit* and answered with
diligence, which is the weaker fix.

Added `**Since:**` to the four true Open items, dated from git history
rather than invented — `git log -S` on each numbered heading. The
spread is the argument for the change:

| item | since | waiting |
|---|---|---|
| 29 Ted invite | 2026-09-02 | 3 days |
| 26 Ship 1.0 | 2026-08-31 | 5 days |
| 27 Probe feedback | 2026-08-31 | 5 days |
| **10 REQUIREMENTS drift** | **2026-07-28** | **39 days** |

Item 10 has been waiting thirteen times longer than item 29 and looked
the same on the card. Cards now read `waiting N days (since …)`.

Made it a **third structural guard** alongside status and Ask: the probe
exits 1 and names the offender if an open item has no valid `**Since:**`.
Verified by mutation (removed one date → refused, naming item 10;
restored → clean). `ageInDays` exported and unit-tested including month
boundary, leap day, and malformed input — a guard whose null path is
untested is a guard that can't fire.

Pattern worth keeping: **when a user reports a symptom, check whether
they are describing a missing signal rather than a missing effort.**
Xian said "some items seemed stale." I heard "audit more carefully."
The brief made me hear "the card doesn't tell me how long."

### Noted, not fixed
Importing `fleet-probe.mjs` in the test file executes it — the script
runs on import and writes `docs/probe/attention-deck-<date>.json` as a
side effect of running the suite. Pre-existing, harmless today (the file
is regenerated anyway), but a test run should not write to the repo.
Filed as cleanup, not done silently mid-task.

## Today's brief (09-05) found a live bug in R1.5

The 09-05 brief arrived while I was processing the 09-04 one. Its lead
insight — *"every new instrument has a cold-start period where
absence-of-data and absence-of-event are indistinguishable, and the
natural phrasing of absence is exactly wrong during it"* — sent me to
check my own instrument, and it was wrong.

`metricsStore.fresh()` set `firstUse: today()`. R1.5 shipped in rc.34,
months after anyone started using One Job, so **firstUse dated the user
from the day the instrument arrived.** Consequences, both plausible
enough to go unquestioned:

- `isActivated` requires 5 cards created. A user with 100 existing cards
  shows `created: 0` and reads as **never activated** until they happen
  to make five more.
- `isRetained` counts active days 22–28 from `firstUse` — the right
  window measured from the wrong origin.

Xian is exactly this user. The very first numbers R1.5 would have shown
him were about a person who started using his own app last week.

**Fix:** ask a history that exists independently of the instrument. The
deck's own cards carry `createdAt`, so the earliest card is a real lower
bound on when use began. `earliestCardDay()` reads `oneJobTasks`
directly — read-only, never through `taskStore` (a cycle), everything in
try/catch so the one hard rule survives. Existing documents repair
themselves once on load.

Two safety properties I made structural rather than intended:
- **Derivation only moves `firstUse` earlier.** A newer deck can never
  shorten someone's recorded history. Test asserts it.
- **`firstUseSource: 'derived' | 'observed'`** so a backfilled value is
  never mistaken for a direct observation — CIO's provenance flag,
  adopted wholesale.

**Verified by mutation, not by green.** The brief's second insight is
that text-matching proves a file changed while only an observable
outcome proves the change took effect. Neutered `deriveFirstUse` back to
the pre-fix behaviour: exactly the two behavioural tests went red, the
seven boundary tests stayed green (correctly — they test the pure
function, which the mutation didn't touch). Restored: 24 green, 206
across the suite, production build clean.

**Third mutation test today.** It is becoming the default way I close a
fix, and it keeps paying: today it converted "the dedupe guard exists"
into "the dedupe guard fires," and "the cold-start fix is written" into
"the cold-start fix is load-bearing."

### The meta-pattern in both briefs
Two days, two findings, same shape: **a signal that reports a state
without reporting its provenance in time.** "Open" without since-when.
"Never invoked" without was-I-installed-yet. "Silent" without
last-ran-when. Each reads as information and is actually a missing
dimension. Worth carrying as a check: *when my code reports an absence,
can it tell "didn't happen" from "not while I was watching"?*
