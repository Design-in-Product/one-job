# Coral Log: 2026-07-25

## Session Start: Saturday, ~reorientation after a 17-day gap

### Context
- Agent: **Coral** — One Job's resident agent. (Continuity = identity;
  whichever model turns the crank.)
- Last session: **2026-07-08**. Confirmed — July 8 log is the most
  recent; no session logs in between. Xian spent the gap DOGFOODING One
  Job to run his daily life. Verdict: "for the most part, I love it
  already," and he's clarified his final pre-MVP wants.
- Branch realigned to origin/main (**7f1fa83**). Main advanced ~14
  commits while I was away — almost all Janus brief deliveries + three
  Janus→Coral memos.
- Version: **1.0.0-rc.9** (unchanged since 07-08).

### Mail read at session start (3 new Janus memos)
1. **MVP blockers (07-24)** — the headline. Xian's final 1.0 blockers,
   after weeks of real use. Four items (see triage below) + a paywall
   sidenote (syncable stored data = clear post-1.0 paywall win).
2. **Promo image refresh (07-23)** — DinP gallery card for One Job is
   stale (unchanged since March, doesn't match current swipe-card UI).
   Ask: fresh 1600×900 PNG of the current look. Not urgent.
3. **Handoff-memo prep + migration heads-up (07-21)** — Claude Desktop
   crashing on Xian's end; possible migration of agents to terminal
   sessions on Amber (Mac Studio) and/or fresh accounts. Ask: Relay and
   I prepare handoff memos, be ready to move on short notice. Prep, not
   emergency. (07-25 brief confirms the hazard: switching Anthropic
   accounts empties Claude Code memory completely — so the handoff
   memos + these logs ARE the continuity across a migration. Extra
   reason to keep them current.)

### Xian's 4 MVP blockers — initial triage (grounded in current code)
1. **Promote/demote cards + per-card action menu at any depth.** The
   big one. Today's long-press menu is DECK-level (the arc menu: Add
   Task/Completed/Integrations/Settings) and fires ONLY from CardDeck —
   SubstackView/TaskStack have no long-press at all. Xian wants a
   per-CARD menu ("tap-hold on any card → things you can do to it"),
   crude-but-functional OK, working at every depth. This is the home
   for vision **Item 8 (promote)** and **Item 25 (make-into-subtask /
   demote)** — they stop being someday-items and become the MVP gate.
   Demote-into-another-card's-subdeck needs a target picker; promote =
   move a sub-card up to be a peer of its parent.
2. **Facing-up on subdeck exit.** Back out of a subdeck → the card you
   land on should be face-up. Small.
3. **Subdeck visibility from the parent, no edit step.** Show a card's
   subdeck on its back and/or make it directly openable from the parent
   — without tapping into details/edit first. Medium; design options
   open. (The mini-deck already exists but lives inside the details
   modal.)
4. **Import as subdeck.** importTasks currently OVERWRITES
   (`this.tasks = migrate(...)`). Want: import an exported deck as a
   NEW subdeck of a card instead. Medium.

### Open thread I need Xian on
- **Relay / TestFlight is dark.** Relay's last log is **2026-07-05**;
  no activity in the 20-day gap, and rc.9 (nor any recent build) has no
  confirmation of reaching TestFlight. The 07-08 handoff noted the flow
  was "still unconfirmed." Need to know where device/TestFlight
  actually stands before treating the cut as imminent.

### Git-identity hygiene (Janus's flagged hazard) — CHECKED, CLEAN
`git log --format="%an <%ae>" -20`: 17 × `Claude <noreply@anthropic.com>`
(mine) + 3 × `mediajunkie` (Xian). No Relay-authored commits, no silent
identity swap of the kind Janus/Themis hit. No action needed.

### Session objectives
1. Reorient (done) + surface the Relay/TestFlight gap.
2. Converge with Xian on the final MVP definition (his 4 + whatever
   "more on that" he adds) and triage blocker-vs-polish.
3. Then build, test-first as ever.

---
## Session #1 cont: MVP blockers clarified + gestural grammar audit

Xian answered all 4 blockers. Key reframe: before building blocker 1's
per-card menu, he wants to RATIONALIZE the gestural grammar first, on a
clean principle: **tap = object's default action; tap-hold = object
reveals its actions.** (Plus my addition: swipe = express lane for the
commonest actions.)

Device reality check: **Xian has been dogfooding the WEB PWA, not
TestFlight.** So the web build is the real MVP surface; TestFlight is a
parallel track still dark (awaiting Relay's status reply). This means
the MVP cut is fundamentally a WEB milestone — native/TestFlight can
follow. Good news: everything I build is verifiable in the exact
surface he uses.

Wrote **docs/GESTURES.md** — accurate current-vocabulary inventory +
the rationalization. Core finding: tap-hold is currently bound to the
DECK and reveals APP nav, not to a CARD revealing its actions — that
mis-binding IS blocker 1. Fix: separate the two objects. Hold a CARD →
card actions (complete/defer/promote/move-into/open-subdeck/edit/
delete), at any depth. Hold the DECK/table → deck actions (add/
completed/integrations/settings). Swipes = express lanes; the hold-menu
is the superset. 4 open questions posed to Xian before I build.

Decisions banked this session:
- Add affordance = **small centered "+"**, consistently (not the wide
  button, not right-aligned). Unambiguous; offered to ship immediately.
- Promote → newest-on-top of new deck.
- Face-up-on-arrival is a nav rule; keep the "eventually flips back"
  touch.
- Sub-deck-from-parent MVP = tappable badge on the face.
- Import: never default to a destructive action without confirmation;
  import-as-subdeck is the safe default, replace-my-deck always
  confirms; receiving card = active/top for now.
- Demote target picker = hierarchical outline (drag to any level).

Also sent: **Relay reconnect/state-sync memo** (07-25), supersedes the
stale rc.4/rc.6 memos, asks the 4 device/TestFlight status questions.
Xian has nudged Relay directly.
