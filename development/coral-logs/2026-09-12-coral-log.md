# Coral Log: 2026-09-12

## Brief processed: self-monitoring circularity (a check inside the
## procedure it watches goes dark with it)

Audited One Job's monitors against the shape:
- **Boot watchdog**: the clean case — it lives in the HTML precisely
  because the bundle is the thing that fails. This brief's principle,
  applied here three days before the brief named it.
- **CI watching**: two layers — my session-start check (internal to my
  discipline, fails with it) + the ci-red-beacon (external, chokepoint-
  attached, fires regardless of my behavior). The beacon is the layer
  that satisfies the brief's test.
- **One honest instance found**: `onejob.co/probe/latest.json` serves
  whatever deck the probe last generated — if the probe doesn't run
  for a week, a stale deck sits at a URL named "latest," a quiet
  default (m-44-shaped: "latest" is a name, not a measurement). Partial
  mitigation already present: every card's description carries its
  dealt date, so staleness is visible ON the cards themselves once
  imported. Accepted for now — the probe runs when the rollup changes,
  and the visible dates are the external surface. Revisit if Xian ever
  imports a stale deck without noticing the dates (the twice-bitten
  threshold).
# Coral Log: 2026-09-12 (evening) — R-INTENT built and uploaded as 1.1 (38)

Janus relayed two directives: R-INTENT is the active lane (Teresa
waiting), and always-move-to-unblocked-work is standing. Executed:

**The Add Card seam, end to end in one evening:**
- Swift AppIntent (tracked master native/ios/AddCardIntent.swift +
  installed into the gitignored Xcode project; pbxproj wired by hand).
  perform() appends to a pending queue in Capacitor Preferences'
  UserDefaults namespace (verified against the plugin source) — it
  never touches the deck.
- shortcutsInbox.ts drains at boot (post-hydration) and on foreground
  through the NORMAL store paths — title invariant, metrics, dedupe of
  failure (a bad card stays queued, visible, retryable; never sinks
  the batch, never silently drops). 7 RED-zone tests, hostile inputs
  first. 224 green.
- Two build lessons, both now written at the failure sites:
  1. **Shortcut phrases can only interpolate AppEnum/AppEntity** — a
     String param in a phrase fails the ENTIRE archive at
     ExtractAppIntentsMetadata. And the first failed archive left the
     STALE build-37 archive on disk, which the export step then
     "successfully" exported — verified the IPA's own Info.plist
     before upload, which is what caught it.
  2. **A released version's train closes.** Build 38 as 1.0 was
     rejected (90186, hidden behind a generic -19241 "server error" —
     I retried a misdiagnosis three times before reading the FULL
     error). Marketing version bumped to 1.1: correct, not a
     workaround — the intent IS the 1.1 lead.
- **1.1 (38) uploaded: Delivery UUID cd530600-ecde-48ad-93c1-f345497fcc3f.**

Next: build processes → TestFlight → device verification (Xian/Ted:
does "Add Card" appear in the Shortcuts app; does Teresa's Reminders
flow land cards). Then Xian tells Teresa her seam exists.

## First tester report: the seam PASSES on a fresh install

Xian, from a parking lot, within hours: Shortcuts → Add Card → "job" →
in the deck. The full path works where it counts.

Siri refused ("hasn't added support") — Janus hypothesized the phrase
never shipped; checked the BUNDLE (m-52 discipline): the provider and
plain phrase DID ship, in exactly the no-param-prompt shape Janus
proposed as the fix. Two hypotheses remain (phrase-index lag vs.
extraction gap); the retest protocol now in the What-to-Test note
distinguishes them. Fair hit absorbed either way: my log recorded what
failed, not what shipped — the release-note practice is the fix for
that shape, and it existed for less than a day before earning its keep.
Discoverability line added for the cohort (+ → Add Action → search).

## The placement ruling — observed at dusk, shipped by midnight

Xian's addendum observation ("the new card was one under the top card
fwiw") + Janus's chosen-vs-defaulted push became a real product ruling
today: **externally-dealt cards never usurp the top.** Investigation
showed the observed behavior was ACCIDENTAL — the code gave intent
cards the top by sortOrder; a UI race produced the second-place he
saw, and the next open could have flipped it. Worst state: not wrong,
inconsistent.

Ruled it the way the philosophy demands: the top card is the job, and
a Shortcut must not interrupt it. `placement: 'behind-top'` in the
store (interface comment carries the ruling for the next implementer),
drain inserts FIFO behind the top, empty decks have no focus to
protect, user-created cards still take the top — you chose those.
Three placement tests; 227 green.

**1.1 (39) uploaded (UUID 46e5bb39), processingState VALID, What-to-
Test set** leading with the ruling in tester language: "cards added
via Shortcuts join the deck behind your current job." The ruling
generalizes: when agent-dealt cards arrive someday, arrival-behind-
the-job is already house law.

Pipeline lesson baked in: asc-whats-new now polls for build
registration (upload→API lag raced the first run) — the retry lives in
the script, not in memory.
