# Coral Log: 2026-08-23

## Session Start

### Context
- Environment: Claude Code, Amber
- Project: One Job
- Git Status: clean
- Current Phase: holding on Xian — white screen investigation, the
  Safari-tab-vs-home-screen-icon storage test, and the data-resilience
  writeup offer, all still open from 08-20.

### Brief processed (via origin/main monitor, task b8b9ecwem)
`docs/briefs/cross-pollination/current.md`, 2026-08-23:

1. **Klatch — a wrong comment about code behavior ("this branch is
   unreachable") can sit undetected for many rounds, since comments
   have no runtime surface to fail against**, unlike code assertions.
   Directly actionable, so did a real check rather than nodding along:
   grepped `src/` for confident unverified-behavior claims
   (unreachable/never/cannot/always/impossible). Found three, in
   `localTaskStore.ts`:
   - "a stale UI can never skip a card down the chain" (line 635) —
     verified: each of the four lifecycle transitions
     (archive/unarchive/trash/restore) has its own `cardRoom()` guard
     clause directly below, self-enforcing, not a distant unverified
     claim.
   - "unreachable (the badge only opens a card's first deck)" (line
     757) — a "HARD LESSON" retrospective note describing a PAST,
     already-fixed bug's behavior, correctly scoped as history, not a
     claim about current code.
   - "an imported copy can never shadow or collide" (line 779) —
     verified against `regenerateIds()`: recursively reassigns a fresh
     `uuidv4()` at every level, card and nested decks alike — the
     guarantee is real and code-backed.
   All three check out; none are Klatch-shaped defects. Real check,
   clean result — not skipped on assumption.
2. **Piper Morgan — concurrent subagents sharing one git working tree
   share one git index, causing staging cross-contamination.** **Not
   applicable** — I run as a single session against this repo, no
   concurrent subagent lanes touching the working tree.

No action items — first finding prompted an actual code sweep (a
small, real instance of the queued-but-never-executed 08-11
Institutional Phantom idea, scoped down to something achievable in one
sitting); came back clean.
