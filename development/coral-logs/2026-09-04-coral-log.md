# Coral Log: 2026-09-04

## Context
- Amber, Opus. Xian on vacation. 1.0 submission-ready and waiting on
  his ~15 min in ASC. Deck at 7 cards.

## 09-03 brief, finished today: refusals that route
Ted Nadeau via Themis: *"if I can't do something, tell me why, and also
who I need to talk to."* In a local-first app the user IS the owner, so
the applicable half is routing — does the refusal name what to do next?

Audited every `throw` in the store. Most already routed ("Only
top-level cards move between decks — promote it first"). **Seven
dead-ended where a real path existed** and now name it: the three
completed-card refusals (→ un-complete it first), the three
lifecycle-chain ones (→ complete/archive/trash it first), and the
deck-not-empty one. The blocked-completion message now says tapping the
card takes you to the blocker — which the UI has done since the item-7
fix, but the message never said so.

Deliberately left: "Unknown deck"/"Task not found" are programmer-error
guards, not user refusals — routing text there is noise. "The last deck
cannot be deleted" genuinely has no path; saying so is honest.

## 09-04 brief — a real latent bug, addressed to us by name
Klatch: *"hoisting a lookup out of a loop silently breaks dedup if the
loop creates what it's looking up."* Checked our import path and found
exactly that shape, **live**:

`importFromSource` built its `known` provenance set ONCE from
`deck.cards` before the loop, then never updated it while pushing new
cards. Two entries sharing an `externalId` **inside a single feed** both
passed the check and both imported — no error, plausible output, wrong
result.

**Wrote the failing test first** and watched it go red
(`['ext-1','ext-2','ext-1']`) before touching the fix — both because
it's data-handling code (RED ZONE) and because 08-30's brief taught
that a check which has never failed proves nothing.

Not theoretical: paginated fetches overlap when upstream data changes
mid-fetch, and the GitHub adapter reads issues across *every* repo —
the same import Xian reports pulling ~1000 cards. So the trigger
conditions are already present in the one integration we ship.

Fix: grow the set inside the loop (one line) + a comment at the site
naming why it must not be hoisted back — because the brief's own
warning is that the *cleanup-shaped* change is what silently reverts
this. Two tests pin it. rc.35, 195/195.

**Second finding** (monitoring should report *when* a thing last ran,
not just "silent"): checked our surfaces. The backup-age nudge already
does this correctly — it distinguishes "never" from "N days ago" rather
than a binary stale/fresh. No gap found.

## Note
Rebased once mid-session: the 09-04 brief landed on origin while I was
committing the refusal work. No conflict, nothing at risk (work was
already committed), pulled --rebase and pushed.

## Xian back (morning) — working the board
**Item 30 CLOSED**: "the roadmap is accurate… my paraphrase was
sloppy." No plan change; 10-then-40 stands as ratified. Moved to
Settled with the reasoning attached, so the next person meeting both
versions finds the resolution rather than the ambiguity. Told Themis
(delivery verified on DinP origin/main) — the flag was useful even
though the answer was "no conflict," since the cheap version of that
failure is someone building on the wrong number.

Deck: 7 → 6 cards.

## Working the board with Xian (late morning)
- **Item 30 closed**: roadmap framing authoritative; his paraphrase was
  shorthand. Themis notified.
- **Item 13 closed**: purge copy stays as written.
- **Item 8 — asking the question dissolved it.** He asked "what is the
  zapier toast, what is zapier?" Investigating rather than explaining
  from memory found the button said "Export Tasks", the toast said
  "Sent to Zapier", and the payload was `{action, timestamp, source}`
  — no task data. My July fix had made the MESSAGE honest while the
  feature stayed a lie; I had been treating the symptom for six weeks.
  Roadmap already had push-export under Explicitly Retired, and unlike
  the GitHub import it was NOT beta-gated. Cut on his approval.
- **Cutting it exposed worse neighbours.** Asana and Todoist sat in the
  same picker collecting real API tokens into a password field with **no
  handler in handleImport at all** — fake spinner, then silence. Surfaced
  rather than silently expanding scope; he approved removal. The
  Integrations view is now honest: real GitHub import + a Demo option
  labeled as demo.
- **New priority from him**: "prioritize the todoist integration asap as
  a 1.1 release feature." Roadmap gains a 1.1 section scoping it —
  GitHub repo-scoping first (a source dumping a thousand cards is the
  counter-advertisement for the lens thesis), then Todoist read-only on
  one list, then write-back after a proven week. Asana stays retired
  (team database, wrong shape — the 08-02 source survey's finding).

rc.36. Deck 6 → 4 cards.
