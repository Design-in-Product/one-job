# Coral Log: 2026-08-22

## Session Start

### Context
- Environment: Claude Code, Amber
- Project: One Job
- Git Status: clean
- Current Phase: holding on Xian — white screen investigation, the
  Safari-tab-vs-home-screen-icon storage test, and the data-resilience
  writeup offer, all still open from 08-20.

### Brief processed (via origin/main monitor, task b8b9ecwem)
`docs/briefs/cross-pollination/current.md`, 2026-08-22:

1. **Klatch — fixing an error can shift the failure to a quieter mode;
   verify what replaced it, not just that the old symptom is gone.**
   Directly relevant given the multi-deck backup fix (08-16). Went and
   checked rather than assumed clean: does `ApiTaskStore` (the remote
   store) have any hidden version of the same active-deck-scoping bug
   `LocalTaskStore.getAllTasks()` had? Grepped — no `getDecks`/
   `activeDeck` concept there at all; its `getAllTasks()` is genuinely
   unscoped, matching the interface's own documentation that remote
   mode is single-deck-only. No quieter twin of the same bug hiding in
   the other store implementation. One real, useful application of
   today's lesson rather than a symbolic one.
2. **Piper Morgan — code emitting on a shared event bus with no
   subscriber yet is a latent hazard; add a test that fails the moment
   one appears.** **Not applicable** — no pub/sub event bus anywhere in
   this codebase; the `TaskStore` interface is direct async calls, no
   emit/subscribe pattern to have a dormant-consumer hazard in.

No action items — first finding prompted a real, useful check (came
back clean); second confirmed genuinely not applicable to our
architecture.
